# 술술다이어리 개선 계획 — 시음 기록 개편 · 버전 관리 · iOS 버그

작성 2026-08-02. 요청 7건을 4단계로 나눈다. 단계마다 커밋하고, 다음으로 넘어가기 전에
브라우저에서 수치로 확인한다.

| # | 요청 | 단계 | 성격 |
| :--- | :--- | :--- | :--- |
| 4 | 아이폰에서 시트가 흔들리는 버그 | **1** | 버그 — 지금 쓰기 불편함 |
| 1 | 상단에 버전 표시 (`v1.0.0`) | **1** | 작음 |
| 2 | 변경 이력 화면 + 보고서 | **1** | 작음 |
| 3 | 초안 검색 DB 확보 | **2** | 데이터 |
| 6 | 향·맛·피니쉬 아이콘 선택 | **3** | 큰 개편 |
| 7 | 가중치 상위 8~12개만 기본 노출 | **3** | 6과 한 몸 |
| 5 | 삼각 레이더 차트 → 내 점수 반영 | **4** | 6·7 위에 얹힌다 |

---

## 1단계 — 버그와 버전 (먼저)

### 1-A. 아이폰 시트 흔들림

**원인.** `document.body.style.overflow='hidden'` 은 iOS Safari 에서 문서 스크롤을
막지 못한다. 문서가 러버밴드로 계속 움직이고, `position:fixed` 인 시트는 iOS 에서
**시각 뷰포트가 아니라 레이아웃 뷰포트**를 기준으로 배치되므로 그 움직임과 어긋나
흔들려 보인다. 키보드가 올라오면 시각 뷰포트만 줄어들어 시트가 키보드 뒤로 밀리거나
튄다. Android·PC 는 `overflow:hidden` 이 실제로 먹어서 재현되지 않는다.

**조치.**
1. 시트를 열 때 **본문을 위치 고정**한다 — `position:fixed; top:-scrollY`.
   닫을 때 원래 스크롤 위치로 되돌린다. iOS 에서 유효한 유일한 방법이다.
2. `.sh-body` 에 `overscroll-behavior:contain` — 내부 스크롤이 끝에 닿았을 때
   문서로 전파되지 않게 한다.
3. `visualViewport` 의 `resize`/`scroll` 을 듣고 시트 높이를 실제 보이는 높이로
   맞춘다(키보드 대응). CSS 변수 하나로 넘긴다.
4. 스크롤 위치 복원이 시트 애니메이션과 겹치지 않도록 순서를 지킨다.

**확인.** iPhone Safari 실기. 이 환경에서는 iOS 스크롤 물리를 재현할 수 없으므로
**코드 검증 + 사용자 실기 확인**으로 나눈다(실록 V-6 원칙).

### 1-B. 버전 표시

`APP_VER` 상수 하나를 두고 헤더 통계 아이콘 옆에 작게 표시한다.
`sw.js` 의 셸 버전과 혼동하지 않도록 **사용자용 버전**은 따로 관리한다
(셸 버전은 캐시 무효화용 내부 번호).

### 1-C. 변경 이력

- `CHANGELOG.md` — 저장소의 정식 기록. 사람이 읽는 보고서.
- 앱 안: 푸터에 아이콘 → 시트로 이력 표시. 데이터는 `CHANGES` 배열
  (버전 · 날짜 · 항목). 오프라인에서도 보이게 앱 안에 둔다.
- 새 버전을 처음 열면 "새 기능" 배지를 한 번 띄운다(플래그 하나).

---

## 2단계 — 초안 검색 DB

**지금 무엇이 안 되는가.** 초안 엔진은 `kb.js` 의 `BRANDS`/`PRODUCTS` 만 본다.
그래서 이미 등록한 139병을 다시 검색해도 찾지 못하고, 리큐르·와인은 데이터 자체가 거의 없다.

**조치.**
1. **내 컬렉션을 첫 번째 검색원으로 쓴다.** 이미 등록한 병과 이름이 맞으면 그
   기록에서 사양·노트를 그대로 초안으로 가져온다. 139병이 즉시 검색 대상이 된다.
   같은 병을 다시 살 때(배치 다른 같은 제품) 가장 자주 쓰인다.
   → 데이터 추가 없이 가장 큰 효과. 여기부터 한다.
2. **리큐르·와인 브랜드를 `kb.js` 에 추가한다.** 주력이 아니므로 "주요 리스트"에
   집중한다 — 캄파리·아페롤·베일리스·쿠앵트로·그랑마니에·샤르트뢰즈·드람뷔이·
   말리부·미도리·калуа 등 통용되는 것과, 와인은 산지·품종 수준으로.
   위스키처럼 제품 단위까지 넣지 않는다(끝이 없다).
3. 검색 실패 시 **무엇을 시도했는지** 보여 준다 — 지금은 조용히 빈손으로 끝난다.

---

## 3단계 — 향·맛·피니쉬 아이콘 선택 (요청 6·7)

**데이터.** 세 축마다 디스크립터 목록을 두고 각각 **가중치**를 매긴다.

```js
const NOTES = {
  nose:   [['바닐라', 10, 'vanilla'], ['셰리', 9, 'sherry'], …],
  palate: [['꿀', 10, 'honey'], …],
  finish: [['긴 여운', 10, 'long'], …],
};
```

- 가중치는 **위스키 리뷰에서 실제로 자주 거론되는 정도**를 기준으로 매긴다.
- 기본은 가중치 상위 **10개**만 보여 준다 — 4열 × 2~3줄. 복잡해 보이지 않는 선.
- `전체 보기` 토글로 나머지를 펼친다. 선택된 항목은 접어도 계속 보인다.

**아이콘.** 디스크립터가 수십 개라 하나씩 그릴 수 없다. **계열별 아이콘 20종**을
만들어 공유한다(과일·베리·감귤·꿀·바닐라·초콜릿·커피·스모크·피트·오크·향신료·
후추·꽃·허브·견과·캐러멜·소금·가죽·흙·크림). 참고 화면도 같은 방식이다.

**기록.** 선택한 항목은 저장 시 해당 필드(`nose`/`taste`/`finish`)에 쉼표로 합쳐
넣는다. 직접 쓴 문장이 있으면 지우지 않고 뒤에 덧붙인다 — 사용자가 쓴 글이
선택으로 사라지면 안 된다.

---

## 4단계 — 삼각 레이더 차트 (요청 5)

- 향·맛·피니쉬 각 **0~10** 을 정삼각형 3축 레이더로 그린다(SVG, 무의존).
- **꼭짓점 드래그로 입력하지 않는다.** 손가락으로 정확히 집기 어렵고 접근성이 나쁘다.
  → 슬라이더 3개로 입력하고, 차트는 결과를 보여 준다. 차트를 탭하면 해당 축이 선택된다.
- **내 점수 반영**: `rating_my = round((향 + 맛 + 피니쉬) / 3 × 10)`.
  자동 계산이지만 **직접 덮어쓸 수 있게** 둔다 — 계산이 내 느낌과 다를 수 있다.
  덮어쓰면 그 사실을 표시한다(초안 배지와 같은 방식).
- 카드·상세에 작은 삼각형을 함께 보여 줄지는 4단계 끝에 판단한다
  (카드는 이미 네 줄이 꽉 차 있다 — 실록 C-3·C-4).

---

## 지키는 것

- 무의존 유지 — 차트 라이브러리를 넣지 않는다(스킬 §4).
- 카드 레이아웃은 건드리지 않는다 — 4줄 고정과 폭 예산은 실측으로 잡은 것이다.
- 새 클래스 이름은 추가 전에 `grep` (실록 C-1).
- 좁은 폭은 눈으로 보지 말고 잰다 — 아이콘 그리드는 360px 에서 4열이 들어가는지 확인.
- 단계마다 커밋. 푸시는 사용자가 한다.
