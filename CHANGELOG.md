# 변경 이력

술술다이어리의 사용자에게 보이는 변화를 기록한다.
앱 안에서도 같은 내용을 볼 수 있다 (헤더의 버전 표시 또는 푸터 → 변경 이력).

버전 번호는 **사용자용**이다. `sw.js` 의 셸 버전(`wc-shell-vNN`)은 캐시 무효화용
내부 번호라 작은 수정마다 올라가고, 이 문서의 번호는 알릴 만한 변화에만 올린다.

기준: `주.부.수` — 주(큰 개편) · 부(기능 추가) · 수(수정).

---

## v2.4.0 — 2026-08-12

### 새로 (라이어 게임 — 일반 상식 제시어)
- 제시어를 **컬렉션의 병 / 향·맛·여운 태그** 외에 **동물 · 음식 · 사물 · 장소 · 직업 ·
  나라 · 삼국지 인물** 중에서 고를 수 있다(요청: "다양한 연령층이 사용할 수 있도록").
  위스키를 모르는 사람이 섞였거나 아이들과 할 때는 이쪽을 고르면 된다.
- 비슷한 것끼리 짝지어야 게임이 된다는 원칙은 그대로다 — `호랑이` 와 `냉장고` 가
  나오면 첫 마디에 끝난다. 그래서 각 카테고리를 **묶음(group)** 으로 나누고 같은
  묶음 안에서만 짝을 뽑는다(병의 타입·지역 좁히기와 같은 구조):
  - 동물 — 반려동물 / 농장동물 / 야생동물 / 조류 / 바다생물 / 파충류·양서류·곤충
  - 음식 — 한식 / 분식 / 디저트 / 과일 / 음료 / 양식
  - 사물 — 주방용품 / 문구류 / 가전제품 / 가구 / 전자기기 / 욕실용품
  - 장소 — 실내시설 / 자연 / 도시시설 / 집안공간
  - 직업 — 의료 / 교육 / 서비스 / 전문직 / 창작 / 기술
  - 나라 — 대륙별(동아시아 · 동남아시아 · 유럽 · 아메리카 · 아프리카·중동 · 오세아니아)
  - 삼국지 인물 — **소속 세력별**(촉 · 위 · 오 · 군웅). 세력이 같으면 "장수다" 같은
    세력을 몰라도 맞는 말로는 못 버틴다.
- 토론 화면의 힌트도 카테고리마다 다르게 뜬다(예: 동물 — "생김새나 사는 곳처럼
  애매한 특징으로 이야기해 보세요").
- 구조: `LIAR_SRC` 배열에 항목만 더하면 되게 만들었다 — 병·태그는 각자의 짝짓기
  함수가 있고, 나머지는 `liarWordPair(catKey)` 하나를 공유한다. 시작 전 안내문은
  `liarHasEnough(src)` 로 판정하는데, 병은 컬렉션 크기에 매이지만 나머지는 큐레이션된
  목록이라 항상 만들 수 있다.

---

## v2.3.1 — 2026-08-11

### 바뀜 (참가자 상한)
- 시음 게임(멀티 블라인드)과 술자리 게임 3개 모두 **최대 참가자를 10명에서 30명으로**
  늘렸다. 동호회 시음회 규모를 고려한 요청.
- 사다리 타기는 30명이면 칸이 30개라 열 간격을 고정(54px)해 두면 폭이 1618px 가
  된다. 열 간격을 인원에 맞춰 30~54px 로 좁히고(`ladCol`), 줄 수도 16줄로 캡을
  둬(`ladRows`) 세로가 끝없이 길어지지 않게 했다. 사다리는 `.padder` 안에서만
  가로 스크롤되고 시트 본문은 넘치지 않는다(실측 확인).
- 발판을 놓는 방식도 바꿨다 — 목표 개수를 정해 두고 무작위 (줄,칸)에 반복해서
  던지는 이전 방식은 30명처럼 밀도가 높아지면 자리가 이미 차 있어 되던지기가
  수백 번씩 헛돌았다. 줄마다 **후보 칸 순서를 섞어 한 번에 훑는** 방식으로 바꿔
  30명까지도 실측 13ms(360회)로 끝난다.
- 사다리 자체의 칸별 도착 확률은 인원이 늘수록(줄 수를 16으로 캡해 뒀으므로) 더
  치우친다(30명 실측 고정점 비율 14%, 균등이면 3.3%). **그래도 실제로 받는 결과는
  균등하다** — 결과 목록을 사다리와 별도로 한 번 더 섞어 넣기 때문이다(v2.3.0 에서
  이미 그렇게 만들었다). 임의의 (치우친) 확률변수를 독립인 균등 난수 순열에 통과시키면
  그 합성은 항상 정확히 균등하다는 성질이다. 30명 3만 회 실측으로도 확인했다
  (기대값 1000, 표준편차 31.1, 30개 표본의 최대 편차 2.12σ — 우연 범위).

### 고침 (사다리 타기 입력칸이 기기 기본 모양으로 뜨던 문제)
- 앱의 입력 스타일은 `.f input` 에 매여 있는데, 사다리 결과 입력칸은 그 밖에 있어서
  스타일을 하나도 못 받고 **OS 기본 입력**이 그대로 나왔다(스크린샷 지적 — 다른 화면은
  다 앱 테마인데 여기만 흰 배경에 각진 기본 테두리였다). `.pslot input` 에 배경·테두리·
  둥근 모서리·포커스 링을 `.f input` 과 같은 값으로 직접 채웠다. 라이트/다크 모두 같은
  CSS 변수를 쓰므로 테마 전환도 그대로 따라간다.

---

## v2.3.0 — 2026-08-11

술자리에서 기기 하나로 여러 명이 하는 게임 3개. 기존 시음 게임과 **성격이 달라** GAME
목록을 `시음` / `술자리` 로 갈랐다.

### 새로 (라이어 게임)
- 모두 같은 제시어를 받고 **한 명만 다른 것**을 받는다. 돌아가며 한마디씩 한 뒤 비밀 투표.
- **제시어가 내 컬렉션에서 나온다** — 이게 이 게임의 전부다. 아무 단어나 쓰면 어느 앱에나
  있는 라이어 게임이지만, `라가불린 16년` 과 `탈리스커 10년` 이면 피트 이야기로 버텨야
  해서 위스키를 아는 사람들끼리 훨씬 재밌어진다.
- 비슷한 것끼리 짝지어야 게임이 성립한다(`라가불린` vs `베일리스` 면 첫 마디에 끝난다).
  병은 **타입 → 지역 → 나머지** 순으로 좁히고, 태그 모드는 아이콘 계열이 같은 것끼리
  묶는다(`토피` vs `캐러멜`). 실측: 6병짜리 예시 컬렉션에서도 `아드벡 10년` vs
  `라가불린 16년`(둘 다 아일라 피트)이 나왔다.
- 라이어에게 **비슷한 다른 술을 주는** 방식과 **아무것도 주지 않는** 고전 방식 둘 다 된다.
  앞쪽은 라이어도 자기가 라이어인 줄 몰라서 연기할 필요가 없다.
- 동표면 라이어가 이긴다 — 지목이 갈렸으면 잡아낸 것이 아니다.

### 새로 (삼각 검사 · 오드원아웃)
- 잔 3개 중 **2개는 같은 술, 1개만 다른 술.** 다른 하나를 찾는다. 전문 관능검사에서 쓰는
  형식이라 찍으면 3분의 1이고, 그보다 뚜렷하게 잘 맞히면 실제로 구별한 것이다.
- **노트가 필요 없다.** 채점 기준이 내 기록이 아니라 "실제로 어느 잔에 무엇을 따랐나"
  이므로 컬렉션의 아무 병이나 쓸 수 있다(시음 게임은 노트가 빈 병을 못 쓴다).
- 따르는 사람은 어쩔 수 없이 답을 알게 되므로 **진행자를 명시하고 답하지 않게** 했다.
  지시는 덮개 뒤에 두고, 따른 뒤 다시 덮은 상태로 넘긴다.
- 어느 병이 1잔인지와 그 잔의 자리를 라운드마다 다시 뽑는다 — 앞 라운드로 추측할 수 없다.

### 새로 (사다리 타기)
- 누가 다음 잔을 따를지, 오늘 어느 병을 열지 정한다. 결과는 직접 적거나 **컬렉션에서
  뽑아** 채운다(비운 병과 위시는 빼고 뽑는다).
- SVG 를 손으로 그린다(`radarSVG`·`qrDraw` 와 같은 방식) — 라이브러리를 쓰면 오프라인에서
  빈 칸이 된다.

#### 사다리에서 실제로 밟은 셋
1. **발판을 왼쪽부터 훑으며 "놓았으면 다음 칸 건너뛰기" 로 만들면 안 된다.** 0 에 놓으면
   1 을 건너뛰고 2 를 보게 되어 발판이 **짝수 칸에 쏠린다.** 같은 인접 교환이 반복되면
   서로 상쇄되어 제자리로 돌아온다 — 5칸에서 항등 순열이 3.45%(무작위면 0.83%) 나왔고,
   실제로 만든 첫 판이 5명 전원 제자리였다. **(줄, 칸)을 무작위로 골라** 하나씩 놓고
   같은 줄에서 옆에 붙는 것만 버리는 방식으로 바꿨다.
2. **발판 개수를 고정하면 순열의 절반이 도달 불가능해진다.** 발판 하나가 인접 교환(홀수
   순열)이므로 개수를 k 로 고정하면 부호가 k 의 홀짝으로 고정된다 — 4칸에서 24개 중
   **12개만** 나왔다(개수가 늘 7개였다). 개수를 4가지 값에서 뽑아 두 부호를 모두 낸다.
3. **"아무도 제자리로 안 오게" 걸러내려 했는데 그러면 2명 사다리가 고정 결과가 된다.**
   2칸에서는 제자리와 교환이 유일한 두 결과라, 제자리를 버리면 늘 서로 바뀐다. 걸러내지
   않고 그대로 둔다.
   대신 **결과를 칸에 섞어 넣어** 사다리 자체의 잔여 편향을 무의미하게 만들었다. 그래서
   사다리 아래쪽에 칸 번호를 적지 않는다 — 적어 넣은 순서와 다르기 때문이다.

   검증: 2~10칸 × 400회에서 순열 깨짐·인접 위반 0건. 4칸 24개 순열 전부 출현. 2칸 교환
   비율 49.7%. 5칸에서 참가자 5명 × 결과 5개 분포의 최대 편차 2.9%(표본오차 수준).

### 고침 (만드는 중에 드러난 것)
- **라이어 토론 시계가 화면을 벗어났다 오면 처음부터 다시 갔다.** 남은 시간을 초로 들고
  `startClock(P.timer)` 를 다시 부르는 구조였다 — 28초가 다시 30초가 되어 **떠났다 오기만
  하면 무한정 끌 수 있었다.** 끝나는 시각(`until`)을 저장하고 남은 시간을 계산하도록 바꿨다.
  앱을 닫았다 열어도 이어지고, 닫아 둔 사이에 시간이 지났으면 바로 투표로 넘어간다.
- **삼각 검사에 `phase` 필드를 두었더니 조용히 어긋났다.** 화면 선택에 쓰이지 않는 필드라
  갱신을 빠뜨려도 아무 일이 없었고, 실제로는 답을 받는 중인데 값이 `'pour'` 로 남아
  저장까지 됐다. 단계는 진행 상황에서 끌어내고 필드를 없앴다.
- **전체 공개 때 사다리가 통째로 강조색 덩어리가 됐다.** 공개된 경로를 다 겹쳐 그렸더니
  10명이면 열 개의 선이 같은 색으로 거의 모든 구간을 덮었다. 경로는 한 번에 하나만 그린다.
- 세리프 본문 폰트는 **옛날식 숫자**라 작게 쓰면 `1` 이 `I` 로 읽힌다. 제시어(`라가불린
  16년` → `라가불린 I6년`)와 사다리 번호 칩을 sans + tabular 로 바꿨다.
- 라이어 결과의 조사 오류(`참가자 1 였습니다` / `참가자 1 가 받은 것`)를 조사 없는 표현으로
  바꿨다. 이름에 숫자가 들어가면 한국어 조사를 붙일 수 없다.

### 구조
- 파티 게임은 **저장소를 분리한다**(`wc.party` ≠ `wc.game`). 한 키를 쓰면 ① 파티 게임을
  손볼 때마다 `GAME_VER` 가 남의 시음 판까지 버리고, ② 시음 판의 `glasses[].ans/cand/mys`
  모양을 전제한 채점·결과·기록 쪽에 종류 분기가 번진다. 진행 중인 판은 여전히 하나뿐이라
  새 판을 시작할 때 다른 종류를 정리한다(`clearAllPlay`).
- 화면은 전부 **상태만 보고 고르는 라우터**(`openPartyResume`)를 지난다. 핫시트는 기기를
  넘기는 중에 잘못 눌리기 쉬워서, 앱을 닫았다 열어도 뒤로가기로 돌아와도 같은 자리로
  복귀해야 한다.
- 토론 시계는 **앱의 그 시계(`startClock`)를 그대로 쓴다.** 타이머를 새로 만들면 시트를
  옮길 때 남아 돌아 남의 차례를 넘긴다(`openSheet` 가 `stopClock` 을 무조건 부른다).
- 세 곳에 복사돼 있던 Fisher-Yates 를 `shuffle()` 하나로 뺐다. 인자를 제자리에서 섞지 않고
  **새 배열을 돌려준다** — 원본 순서를 다시 쓰는 호출부가 있다.
- `GAMES` 배열 리터럴이 인원 상수를 읽으므로 상수를 그보다 위에 두었다(실록 V-8 의 TDZ).

---

## v2.2.0 — 2026-08-11

사용자가 올린 **실제 기록 130병의 향·맛·여운 원문**을 앱의 `matchTags` 에 그대로 통과시켜
무엇이 안 잡히는지 조각 단위로 셌다. 측정은 `index.html` 의 사전·매처를 **한 글자도 고치지
않고 잘라 낸 페이지**에서 돌렸다 — 재구현하면 결과가 실제 앱과 어긋난다.

### 고침 (세 축의 어휘 비대칭)
- **원인은 단어가 부족한 게 아니라 축마다 목록이 달랐던 것이다.** 노트를 쓰는 사람은 같은
  향을 향·맛·여운에 다 적는데, 칩은 한 축에만 있어 나머지가 통째로 새어 나갔다. 미스가
  정확히 그 비대칭 지점에 몰려 있었다:
  - `과일`·`토피`·`몰트`·`스파이시` → **맛에만** 있었다 (향에서 `붉은 과일` 9건,
    `토피` 9건, `몰트` 7건, `은은한 스파이스` 5건이 새어 나갔다)
  - `달콤한`·`부드러운` → **여운에만** 있었다 (맛에서 `부드러운 단맛` 7건,
    `부드러운 질감` 계열이 전부 새어 나갔다)
  - `바닐라`·`꿀`·`크리미`·`초콜릿`·`토피` → **여운에 없었다**
  - `자두` 는 13건인데 **어느 축에도 없었다**
- 길이·마무리 성격의 말(`긴/짧은/중간 여운`, `깔끔한`, `복합적인`)만 여운 전용으로 남기고,
  향미 디스크립터는 세 축에 모두 두는 것으로 규칙을 정했다. **85개 → 105개**
  (향 30→38, 맛 30→34, 여운 25→33).

| 축 | 조각 커버율 | 필드당 평균 태그 |
| :--- | :--- | :--- |
| 향 | 74.6% → **96.3%** | 3.22 → **4.52** |
| 맛 | 86.0% → **96.7%** | 3.62 → **4.52** |
| 여운 | 86.5% → **96.2%** | 2.76 → **3.32** |

### 새로 (동의어)
- `견과`(9건)·`짭조름`·`해조`·`해수`·`브라인`·`염습지`·`정향`·`클로브`·`넛메그`·
  `버터스카치`·`흑설탕`·`만다린`·`월넛`·`커피콩`·`밀크초콜릿`·`시리얼`·`곡물`·
  `버터리`·`실키`·`벨벳`·`건조함`·`긴 길이`·`중상`·`헤더`·`ash`·`숯불`, 그리고
  `망고`·`파인애플`·`파파야`·`복숭아`·`바나나` → `과일`.
- `견과` 가 안 잡힌 이유가 규칙의 경계였다: 어형 비교는 낱말이 3글자 이상일 때만 도는데
  `견과` 는 2글자라 `견과류` 에 닿지 못했다. 동의어로 직접 넣었다.
- **한 글자 변형은 여전히 넣지 않는다.** `귤` 을 넣으려다 뺐다 — `배` 가 `배럴` 에 걸리는
  것과 같은 사고다(이미 `감귤`·`오렌지` 로 잡히므로 넣을 이유도 없었다).

### 고침 (안내에 박혀 있던 `84개`)
- 게임의 후보 좁히기 안내가 `84개` 라고 **손으로 박혀** 있었다. v2.0.0 에서 `중간 여운` 을
  더해 실제로는 85개가 된 뒤에도 안내는 84개로 남아 이미 어긋나 있었다. `NOTE_ALL` 로
  파생값을 만들어 네 곳(중급 설명, `optDesc`, 셋업 버튼, 패널 항목)이 목록에서 수를
  읽게 했다 — 칩을 더해도 다시 어긋나지 않는다.

### 게임
- 채점할 수 있는 병이 **129 → 130개(전부)** 가 되었다. 축이 비어 채점 불가였던 병이 사라졌다.
- `GAME_VER` 5 → 6. 정답(`ans`)과 후보(`cand`)는 판을 만들 때 고정되는데 격자는 새 목록으로
  그려지므로, 진행 중이던 옛 판은 기준과 화면이 어긋난다 — 이어 받지 않고 버린다.
- **고급 난이도의 구조적 약점을 측정으로 확인했다(이번에 바꾸지 않음).** 고급은 문턱이
  0.9라 동의어(0.85)·어형을 인정하지 않는데, 한국어 산문 노트는 그 층에 크게 의존한다.
  결과적으로 고급에서 여운 정답이 병당 평균 **1.72개**(보통 3.32개)이고, 130병 중
  **22병은 여운 정답이 아예 비어** 채점 대상에서 빠진다(고급 채점 가능 107병).
  가장 흔한 표현인 `중간 길이의 마무리` 조차 동의어 경로라 고급에서는 정답이 아니다.
  문턱을 0.85로 내리면 보통(0.75)과 실질적으로 같아진다 — `MATCH_W` 에 0.75와 0.85
  사이 층이 없기 때문이다. 난이도 설계를 건드리는 일이라 측정만 남기고 미뤘다.

### 바뀜 (편집 화면 기본 펼침)
- 향·맛·여운과 `상세 사양 · 구매 정보 · 태그` 가 **펼친 상태로 열린다**(사용자 요청).
  접혀 있으면 편집할 때마다 `수정`·`상세 사양` 을 한 번 더 눌러야 했다. 접이식은 그대로
  남아 있어 다 채운 뒤에는 `모두 접기` 로 짧게 쓸 수 있다.
- `모두 수정` 버튼의 글자는 `syncAllBtn` 이 상태에서 만들어 내므로 `hidden` 만 빼면
  `모두 접기` 로 알맞게 뜬다 — 라벨을 따로 손대면 상태와 어긋난다.

---

## v2.1.1 — 2026-08-11

### 바뀜 (게임 화면 드롭다운)
- **v2.1.0 은 목록 화면의 주종·타입만 고쳤다.** 게임 설정·평가 화면의 참가자 수,
  제한시간, 후보 좁히기, 오답 감점, 예상 병, 예상 지역은 여전히 네이티브 `<select>`
  라 안드로이드에서 똑같이 전체화면 피커가 떴다("게임쪽 드롭다운은 휴대폰에서
  여전히 동일하게 꽉차는 문제 있음" — 실사용자 지적). 이 6곳 모두 같은 커스텀
  패널(`openDropMenu`)로 바꿨다.

### 고침 (시트 안 패널이 안 보이던 버그)
- 커스텀 패널의 `z-index` 가 45, 게임 설정·평가가 뜨는 시트(`.sheet`)는 50이었다.
  목록 화면의 주종·타입 버튼은 시트 밖에 있어 문제가 없었지만, **시트 안에서 여는
  패널은 시트의 불투명한 배경 뒤에 완전히 가려 안 보였다** — 값은 정상 저장되는데
  화면에는 아무것도 뜨지 않는 것처럼 보이는 조용한 버그였다. 패널을 55로 올려
  시트보다 위, 라이트박스(80)·토스트(90)보다는 아래에 오도록 정리했다.
- 같은 이유로, 패널을 고른 직후(150ms 페이드아웃이 끝나기 전) 다른 트리거를 곧바로
  열면 **사라지는 중인 옛 패널을 새로 연 것으로 착각**해 엉뚱한 목록을 보여 주는
  경우가 있었다(예: 참가자 수를 고른 직후 후보 좁히기를 열면 "2명~10명" 이 보임).
  이제 패널을 열 때마다 남아 있는 `.dropmenu` 를 전부 즉시 치운다.

---

## v2.1.0 — 2026-08-10

### 바뀜 (주종·타입 드롭다운)
- **네이티브 `<select>` 를 앱 테마 그대로인 커스텀 패널로 바꿨다.** 안드로이드에서
  드롭다운을 탭하면 OS 가 전체화면 피커를 그리는데 다크 테마를 전혀 못 입혔고,
  PC 에서는 작은 인라인 목록인데 폰에서는 화면을 꽉 채워 **같은 컨트롤이 플랫폼마다
  다른 크기로 보였다**(실사용자 지적). 이제 버튼 바로 아래에 뜨는 작은 패널이라
  어느 기기에서나 크기가 같고, 라이트/다크 테마가 그대로 적용된다.
- 항목이 많아도(타입 17종 실측) 패널이 화면 안에서 스크롤된다. 화면 오른쪽·아래로
  넘칠 것 같으면 안으로 접어 넣는다.

### 바뀜 (주종 — 0건도 항상 표시)
- **주종이 하나뿐이어도 드롭다운이 사라지지 않는다.** 예시 6병이 전부 위스키뿐이면
  전에는 "주종" 드롭다운 자체가 없어졌다 — 사용자 입장에서는 이 앱이 위스키 말고도
  기록할 수 있다는 사실 자체가 안 보였다("전체 탭이 0병이어도 늘 보인다"는 이 앱의
  기존 원칙과 같은 이유). 이제 `와인 0` `리큐르 0` 처럼 0건도 그대로 나열되고,
  그중 하나를 고르면 "0건" 결과 화면으로 정상 이동한다.

---

## v2.0.0 — 2026-08-09

### 새로 (텍스트 → 태그 자동 매칭)
- **자유롭게 쓴 시음 노트에서 향·맛·여운 태그를 찾아 켜 준다.** 전에는 쉼표로 자른
  조각이 디스크립터와 **완전히 같을 때만** 켜졌다. 사용자 실제 컬렉션 146병으로 재 보니
  노트를 쓴 **381칸의 52%가 태그를 하나도 못 켜고 있었다.** 지금은 **0%**다
  (칸당 평균 0.80개 → 2.92개).
- **다르게 쓴 말도 알아본다** — `감귤류`·`오렌지` → `시트러스`,
  `스파이스` → `스파이시`, `매끄러운` → `부드러운`, `길고` → `긴 여운`.
- **임베딩 API 를 쓰지 않는다.** 대상이 고정된 84개 단어라 의미 유사도 문제가 아니라
  사전 매칭 문제다. 서버도 키도 없이 오프라인에서 즉시 끝난다.
- **찾은 태그는 `자동 등록` 줄에 따로 보여 준다.** 내가 쓴 문장에 합쳐 넣지 않는다 —
  섞이면 무엇을 내가 적었고 무엇이 자동으로 붙었는지 나중에 구분할 수 없다.
- 확신이 낮은 것(0.5~0.75)은 켜지 않고 **점선으로만** 추천한다.
- **여운에 `중간 여운` 을 더했다.** 실제 노트에서 `중간 길이의 마무리` 가 가장 흔한
  표현인데 길이 칩이 긴·짧은 둘뿐이라 **고를 것이 없었다.** 매칭이 아니라 목록의 구멍이었다.

### 바뀜 (게임 정답 기준)
- **게임 정답이 카드에 보이는 태그와 같아졌다.** 게임에만 별도 추출기가 있어
  아이콘 계열까지 넓혔고, 그래서 문장에 없는 태그가 정답에 들어갔다 —
  글렌피딕 `서양배, 풋사과, 옅은 오크 바닐라, 꽃향` 에 **무화과·장미**가 붙었다
  (`꽃`과 같은 계열이라서). 이제 편집 화면과 같은 기준을 쓴다.
- **고급 난이도는 동의어·어형을 인정하지 않는다**(점수 문턱 0.9). 초급·중급은 0.75.
- 진행 중이던 게임 한 판은 초기화된다 — 채점 기준이 바뀌었기 때문이다.
  **저장해 둔 지난 기록은 그대로 남는다**(정답이 기록 안에 박혀 있다).

### 고침
- **사진을 확대한 뒤 닫으면 목록으로 튕겼다.** 확대 창을 닫는 처리가 뒤로가기를
  부르는데, 그 시점에 이미 확대 창이 닫혀 있어 **상세 화면까지 함께 닫혔다.**
- AI 채우기가 접힌 칸을 채우면 **그 칸을 펼쳐 보여 준다.** 19칸을 채우는데 그중
  16칸이 접힌 안에 있어 무엇이 바뀌었는지 보이지 않았다.

### 바뀜 (편집 화면)
- 향·맛·여운을 **`모두 수정`** 으로 한 번에 펼친다.
- 사진 도구에서 **네이버를 뺐다**(쇼핑 검색이라 병 사진으로 쓸 것이 거의 없었다).
  버튼 넷이 한 줄에 들어간다.
- `AI` 버튼이 제목 옆으로 올라가 본문에서 한 줄이 사라졌다.

---

## v1.8.0 — 2026-08-08

### 바뀜 (편집 화면 재구조화)
- **편집 화면이 절반보다 더 짧아졌다 — `2748px` → `1013px`.**
  등록을 마친 뒤 가장 자주 바꾸는 **상태·개봉일·잔량·보관 위치**가 폼 맨 아래
  `@2299px` 에 있어 **2.2화면**을 내려야 닿았다. 지금은 **0.81화면**이다.
- 늘 펼쳐져 있던 셋을 접었다 — 사진 도구(311px, 사진 옆 카메라 버튼으로 연다),
  향·맛·여운 아이콘 그리드(657px, `수정` 으로 편다),
  상세 사양·구매 정보(956px, 접이식).
- **지역/도수/숙성**과 **구매가/시장가 하단·상단**이 한 줄에 셋씩 들어간다.
  값이 짧은데 2열이라 각각 두 행을 쓰고 있었다.

### 없앰
- **Whisky Bible 평점을 없앴다.** 판본마다 점수가 달라 관리 비용에 비해 쓰이지
  않았다. 평점은 **Whiskybase 와 내 점수** 두 가지다. 저장된 값도 지운다.
- 내보낸 CSV 를 다시 가져올 때 **Whiskybase 점수가 Bible 점수로 둔갑하던** 문제도
  함께 고쳐졌다(열 순서는 그대로다).

---

## v1.7.0 — 2026-08-08

### 새로
- **게임이 끝나면 `기록 저장`.** 지난 기록은 `GAME` 목록 맨 아래에서 다시 본다 —
  친구들과 논평하기 위한 것이다. **최대 20개**, 넘치면 오래된 것부터 지워진다.
- 기록에는 **병 이름이 문자열로 박혀 있어** 병을 지워도 그대로 남는다.
  그때의 난이도·배점 규칙도 함께 담아 점수가 나중에 달라지지 않는다.

### 고침
- 멀티에서 아무것도 고르지 않아도 **0점으로 넘길 수 있다.** 시음회에서 "모르겠다"는
  정당한 답이고, 막으면 한 사람 때문에 판 전체가 멈춘다.

---

## v1.6.0 — 2026-08-08

### 새로
- **한 판에 최대 20잔.** 채점할 수 있는 병 수까지가 실제 상한이다.
- **멀티 블라인드 — 서버가 필요 없다.** 기기 하나를 돌려 쓴다(2~10명).
  잔 A 를 모두가 차례로 평가한 뒤 잔 B 로 넘어간다.
- 차례마다 **빈 양식으로 시작하고 넘겨주기 화면이 끼어든다** — 앞 사람의 답이 화면에
  남은 채로 기기를 넘기면 공정성이 무너진다.
- **제한시간 30초~10분.** 끝나면 **입력한 것을 저장하고** 다음 사람으로 넘어간다.
- 결과는 **참가자별 순위표**. 이름을 누르면 잔별 채점 상세가 열린다.

---

## v1.5.0 — 2026-08-08

### 새로 (블라인드 테이스팅 게임)
- 상단 **`GAME`** 버튼. 세 가지 모드가 있다.
  - **셀프 테스트** — 병 하나로 바로. 향·맛·여운을 매기면 내가 기록해 둔 노트와 맞춘다
  - **셀프 블라인드** — 2~6병을 잔에 붓고 어느 잔이 어느 병인지 맞힌다
  - 멀티는 v1.6.0 에서 열렸다
- **정답은 내가 기록해 둔 노트다.** 공식 테이스팅 노트를 쓰려 했지만 내장 DB 제품
  229개 중 노트가 있는 것이 **3개**뿐이었다. 그래서 게임의 질문이 *"이 위스키의
  객관적 노트를 맞혀라"* 에서 ***"내가 적어 둔 내 감각을 내가 다시 알아맞히나"*** 로
  바뀌었다 — 셀프 모드에서는 이쪽이 맞다.
- **난이도 4단** — 초급(후보를 좁혀 주고 빗나가도 깎지 않는다) · 중급 · 고급 ·
  직접(규칙을 하나씩 켜고 끈다).
- **맞힐 항목을 고른다** — 이름 · Whiskybase 점수 · 도수 · 숙성연수 · 지역.
  켠 항목의 배점이 **합 100** 이 되게 자동으로 나뉜다.
- 향·맛·여운은 **몇 개를 골라도 된다.** 맞히면 그 축의 배점을 정답 개수로 나눈 만큼
  올려 주고, 빗나가면 그 **1/3** 만큼 깎는다.
- 막히면 축 머리의 **힌트** — 정답 하나를 켜 주고, **그 하나는 점수에 넣지 않는다.**
- **게임 점수는 컬렉션의 평점·잔량·통계에 영향을 주지 않는다.** 셀프 테스트 결과는
  누르면 시음 기록으로 옮길 수 있다.

---

## v1.4.1 — 2026-08-08

### 고침
- **PC 에서 창을 좁히면 타입 칩의 절반 이상에 마우스로 닿을 수 없었다.**
  칩 행이 가로 스크롤이었는데 스크롤바를 숨겨 두었고 휠은 세로만 구른다.
  520px 창에서 실측: 칩 11개 · 내용 폭 1210px · 보이는 폭 488px —
  **722px 어치가 도달 불가**였다. Shift+휠은 되지만 알 수 있는 방법이 없다.
  → **타입을 드롭다운으로 바꿨다.** 폭에 상관없이 전부 고를 수 있다.
  이제 상단에 가로 스크롤이 남아 있지 않다.

### 바뀜
- 상단 순서를 **술장 탭 → 검색 → 주종·타입**으로 되돌렸다(탭이 맨 위).
- **조건별 필터 칩 행을 없애고 `↺ 초기화` 버튼 하나로 바꿨다.** 그 행은 조건이
  걸렸을 때만 나타나 **48px** 을 먹었는데, 조건이 걸린 상태가 오히려 목록을 더 보고
  싶은 상황이다. 조건이 걸려도 이제 화면이 밀리지 않는다(230px 고정).
  개별 해제는 필터 시트에서 한다.
- 초기화는 **검색어·필터·정렬만** 푼다. 술장 탭과 주종·타입은 화면에 그대로 보이는
  상태라 함께 지우면 "내가 건드리지 않은 것이 사라졌다"가 된다.
- 결과 개수는 3행의 보기 전환 버튼 왼쪽에 작게 (`6개`). 조건이 걸렸을 때만 나온다.
- 주종·타입 드롭다운은 고를 것이 하나뿐이면 자리를 비운다.

---

## v1.4.0 — 2026-08-08

### 바뀜 (상단 영역 정리)
- **상단 제어를 다섯 줄에서 세 줄로.** 폰에서 첫 항목까지 **347px → 232px**
  (뷰포트의 43% → 29%). 리스트에서 보이는 항목이 **5.3개 → 6.7개** 로 늘었다.
  | | 전 | 후 |
  | :--- | :--- | :--- |
  | 1행 | 술장 탭 | **검색 + 필터 + 보기 전환** |
  | 2행 | 검색 | 술장 탭 |
  | 3행 | 주종 칩 + 타입 칩 | **주종 드롭다운(고정) + 타입 칩(스크롤)** |
  | 4행 | 필터 · 정렬 · 보기 | — (1행으로 합침, 정렬은 필터 안으로) |
  | 5행 | 통계 요약 줄 | — (삭제) |
- **정렬이 필터 시트 안으로 들어갔다.** 시트 이름도 `필터 · 정렬` 이고 정렬이 맨 위다.
  기본(최근 등록순)이 아니면 필터 버튼 배지에 함께 세고, 목록 위에 해제할 수 있는
  칩으로 보여 준다 — 그러지 않으면 정렬이 걸린 것을 화면에서 알 수 없다.
- **주종이 칩에서 드롭다운으로.** 칩 여러 개가 타입 칩과 한 줄을 다투면서 주종이
  스크롤 밖으로 밀려 나갔다. 드롭다운은 왼쪽에 고정되고 타입 칩만 스크롤한다.
  **병 수는 옵션 라벨에 넣어 살렸다** — `위스키 (127)`.
  타입 칩 오른쪽 끝은 흐리게 처리해, 더 있다는 것을 스크롤바 없이 알린다.
- **목록 위 통계 요약 줄을 없앴다**(v1.3.0 에서 넣었던 것). 접혀도 39px 을 먹었고,
  그것이 다섯 번째 행이었다. 통계는 상단 **그래프 버튼**과 데스크톱 사이드바에 남는다 —
  사이드바는 세로 공간을 먹지 않는다.
- 결과 개수는 **검색·필터·정렬이 걸렸을 때만** 조건 칩과 같은 줄에 나온다.
  평상시 이 줄은 존재하지 않는다(0px).

> 행 높이(44 / 46 / 40px)는 더 줄이지 않았다. 컨트롤이 38px 이고 그 위에 44px 히트
> 영역을 얹은 값이다 — 40px 미만 컨트롤 15개를 찾아 고친 접근성 감사의 결과라
> 여기서 되돌리면 그 작업이 무효가 된다.

---

## v1.3.0 — 2026-08-07

### 새로 생김
- **술장 탭 — 전체 · 구매 · 경험 · 위시.** 탭 하나가 하나의 술장이다. 카드는 **자기
  상태(미개봉/오픈/비움/경험/위시)에 따라** 자기 술장에 들어가고, 상태를 바꾸면 술장을
  옮긴다. 축이 하나이므로 별도 필드도 이동 UI도 없다 — 전부터 있던 상태 값이 그대로
  의미를 갖는다.
  - `구매` 술장은 **미개봉 + 오픈 + 비움**이다. "지금 갖고 있는 것"이 아니라
    **"내가 산 모든 병"** 으로 정의했다. 구매액·연도별 구매 집계가 자연스럽게 맞고,
    다 마신 병도 컬렉션 이력으로 남는다.
  - 탭 배지는 **컬렉션 전체** 기준이라 검색·필터에 흔들리지 않는다. 대신 **주종 칩의
    숫자는 술장에 따라 달라진다**(의도한 것이다 — 각 축은 자기 부모까지만 반영한다).
- **술장별 통계.** 통계가 보고 있는 술장 기준으로 나온다. 전에는 화면에서 무엇을 보고
  있든 컬렉션 전체 숫자만 나왔다.
  - 목록 위에 **한 줄 요약**이 있고 누르면 펼쳐진다. 펼침 상태는 기억한다.
    (KPI 격자를 늘 펼쳐 두면 폰에서 첫 카드가 1,600px 밀린다 — 접힘이 기본이다.)
  - `경험` 술장에는 **구매 후보** — 마셔 봤고 아직 사지 않은 병을 평점순으로.
    같은 이름으로 구매한 기록이 있으면 빠진다(마셔 본 병을 나중에 사면 기록이 두 개가
    되므로, 그냥 두면 **이미 산 술을 계속 추천한다**). 마셔 본 곳 분포도 함께.
  - `위시` 술장에는 **예상 지출** — 시장가 중간값의 합, 평균 예상가, 가장 비싼 병.
  - 사지 않은 병에는 구매가·잔량·개봉일이 없다. 그래서 경험·위시 술장은 같은 KPI
    격자를 대상만 바꿔 보여 주지 않는다 — 열두 칸 중 여덟이 `0원`·`—` 가 되고
    그 화면은 "통계가 고장났다"로 읽힌다.

### 바뀜
- **주종(위스키·와인·리큐르·기타주류)이 검색 아래 칩으로 내려갔다.** 술장 탭이 그
  자리를 쓴다. 탭은 "이 병이 내 것인가", 주종·타입은 "이 병이 무엇인가"라 층이 다르다.
  화면 줄 수는 늘지 않았다.
- **필터의 `보유만` → `지금 보유`**(미개봉 + 오픈). 구매 술장이 비움을 포함하게 되면서
  "지금 술장에 있는 것만" 보는 수단이 필요해졌다. 상태 필터는 단일 선택이라
  미개봉과 오픈을 함께 고를 방법이 없으므로 이 항목이 그 조합을 담당한다.
- 필터의 `유경험`·`위시리스트` 를 없앴다 — 술장 탭이 그 일을 한다.
- 경험·위시 술장에서는 `잔량순`·`개봉일순`·`가성비순` 정렬과 상태 필터를 숨긴다.
  값이 없어 눌러도 아무 일이 없거나, 술장과 모순돼 항상 0건이 되는 조합이다.
- 통계 KPI `보유 병수` → **`구매 병수`**. 대상이 미개봉+오픈+비움이라 이름이 틀려
  있었다(다 마신 병을 보유로 세고 있었다). 부제에 `지금 보유 n병`을 함께 적는다.

### 고침
- **사이드바 요약이 보유 개수를 잘못 세고 있었다.** 경험·위시까지 포함한 전체 개수를
  '보유'라고 적어, 같은 앱의 통계 화면(`보유 병수`)과 **다른 수**를 보여 주고 있었다.
  이제 요약 줄·통계 시트·사이드바가 **같은 함수**를 쓰므로 어긋날 수 없다.
- **타입 필터에 자동 복귀가 없었다.** 범위 밖 타입은 칩이 그려지지 않는데 필터는 계속
  걸려서 결과가 0건이 되고, 화면에는 풀 방법이 보이지 않았다. 술장 전환이 이걸 드러냈다.
- 주종 되돌리기가 칩을 **그린 뒤에** 실행돼서, 켜진 칩과 목록이 어긋나는 순간이 있었다.

---

## v1.2.1 — 2026-08-07

### 바뀜 (모양)
- **앱 아이콘을 새로 그렸다.** '술술' 위스키병 → **잔에 위스키를 따르는 선 그림**과
  `SULSUL` 워드마크. 배경은 평면 검정이고 잔 뒤에만 따뜻한 빛을 둔다.
  워드마크는 앱 안의 워드마크와 **같은 서체**(Cormorant Garamond)를 쓴다 — 빌드할 때
  `index.html` 의 `@font-face` 를 그대로 들어내 심으므로 둘이 어긋날 수 없다.
- **실행 화면(iOS launch image) 5종도 다시 만들었다.** 실행 화면에는 아이콘이 그림으로
  박혀 있어서 아이콘만 바꾸면 홈 화면과 실행 화면의 아이콘이 서로 달라진다.
- 아이콘 6종 합계가 오히려 **114KB → 90KB** 로 줄었다. 배경을 전면 그라디언트에서
  평면으로 바꾼 결과다(그라디언트는 모든 픽셀이 달라 PNG 가 줄이지 못한다 —
  전면 그라디언트일 때 `icon-512.png` 하나가 150KB 였다).

### 고침
- **폰에서 추가·편집 창이 자판에 덮이던 문제.** v1.1.0 에서 `visualViewport` 높이를
  `--vvh` 로 넘겨 시트 **높이**를 줄여 놨지만, 그것만으로는 부족했다.
  `position:fixed` 의 `bottom` 은 레이아웃 뷰포트 기준이라 시트 아래쪽 — 마지막
  입력란과 **저장 버튼** — 이 자판 뒤에 그대로 남았다. 자판이 가리는 높이를
  `--vvb` 로 따로 넘겨 시트를 그만큼 들어 올린다. 뷰포트 메타에
  `interactive-widget=resizes-content` 를 넣어 Android 는 레이아웃 뷰포트가 함께
  줄어들게 하고, 그 속성을 모르는 iOS 는 `--vvb` 가 대신 처리한다 — 한 식으로 둘 다 된다.
  (360×760 에서 자판 340px 을 가정해 실측: 고치기 전 저장 버튼 692~760px = 완전히 가림,
  고친 뒤 352~420px = 자판 바로 위.)
- **누른 입력칸이 자판 뒤에 남던 문제.** 폼을 이미 스크롤해 둔 상태에서 아래쪽 칸을
  누르면 시트 높이가 줄어도 그 칸이 보이는 영역 밖에 있었다. 자판이 올라온 뒤
  (`visualViewport` 의 `resize` 시점) 그 칸을 보이는 자리로 끌어올린다.
- **목록 끝에서 화면이 흔들리던 문제.** `html,body` 에 `overscroll-behavior-y:none`.
  iOS 의 러버밴드와 Android 의 당겨서 새로고침을 함께 막는다. 시트 안쪽은 전부터
  `overscroll-behavior:contain` 으로 막혀 있었지만 문서 자체는 열려 있었다.

### 바뀜
- **시음 기록의 향·맛·여운 슬라이더가 70 에서 시작한다.** 실제 평가가 대체로
  70점대에 모여 있어 0 에서 시작하면 매번 세 번을 끌어올려야 했다.
  대신 슬라이더를 건드리지 않고 저장하면 70/70/70 이 기록된다 —
  점수를 비워 둔 기록을 만들려면 세 축을 0 으로 내려야 한다.
- **보관 위치를 아이콘에서 고른다.** 자유 입력이던 칸을 **집 · 바 · 기타** 세 개의
  아이콘 셀로 바꿨다. 한 병은 한 곳에 있으므로 하나만 켜지고, 같은 것을 다시 누르면
  해제된다. 전에 자유 입력으로 적어 둔 값은 지우지 않고 그대로 두었다가 칩을 누를 때
  대체한다(무엇이 적혀 있었는지 칩 아래에 보여 준다).
  이 값은 지금까지 **저장만 되고 어디에도 보이지 않았다** — 상세 화면의 스펙 표에 넣었다.
- **상단바의 병 수 표기를 없앴다.** 주종 세그먼트와 통계가 같은 수를 이미 보여 준다.

---

## v1.2.0 — 2026-08-02

### 새로 생김
- **시음 기록의 3축 평가.** 향·맛·여운을 각각 **0~100** 으로 넣으면 삼각 그래프로
  보이고, **세 값의 합 ÷ 3** 이 내 점수로 들어간다. 자동 계산이지만 직접 덮어쓸 수
  있고, 그 뒤로는 자동으로 바뀌지 않는다. 3축 값은 기록에 함께 저장된다.
  (꼭짓점 드래그는 넣지 않았다 — 손가락으로 정확히 집기 어렵고 화면 낭독기로
  다룰 수 없어, 슬라이더로 입력하고 그래프는 결과를 보여 준다.)
- **향·맛·여운 아이콘 선택.** 디스크립터 84개를 아이콘 격자에서 고른다.
  자주 쓰는 10개가 먼저 보이고 '전체 보기' 로 나머지를 펼친다. 고른 항목은
  접어도 남는다. 직접 쓴 문장은 지우지 않고 아래에 그대로 둔다.
- **이미 등록한 병이 초안 검색에 잡힌다.** 같은 병을 다시 살 때 내 기록의
  사양·노트를 그대로 가져온다(구매 정보·사진은 새로 넣는다). 내장 DB 보다
  내 기록이 정확하다는 판단이다. 이름이 완전히 같을 때만 쓴다.
- **리큐르·브랜디·럼·진·보드카·와인 39종** 검색 추가 — 캄파리·아페롤·베일리스·
  쿠앵트로·예거·깔루아·헤네시·바카디·봄베이·앱솔루트·샴페인·보르도·포트·셰리 등.
  주종도 자동으로 갈린다. 제품 단위는 넣지 않았다.

---

## v1.1.0 — 2026-08-02

### 고침
- **아이폰에서 입력창을 열면 화면이 흔들리던 문제.** `body{overflow:hidden}` 은
  iOS Safari 에서 문서 스크롤을 막지 못하고, `position:fixed` 인 시트는 iOS 에서
  시각 뷰포트가 아니라 레이아웃 뷰포트를 기준으로 배치돼 문서의 러버밴드와
  어긋나 떨렸다. 본문을 `position:fixed` 로 고정하고, 내부 스크롤 전파를 막고
  (`overscroll-behavior:contain`), 키보드가 올라오면 `visualViewport` 높이에
  시트를 맞추도록 바꿨다. Android·PC 에서는 재현되지 않던 버그다.
- 리스트 보기에서 잔량이 있으면 가격이 사라지던 문제.
- '위시' 표식이 '미개봉' 과 같은 초록으로 보이던 문제.

### 바뀜
- **앱 아이콘**을 '술술' 라벨이 붙은 위스키병으로 새로 그렸다.
  (이미 설치한 기기는 삭제 후 재설치해야 새 아이콘이 보인다.)
- **카카오톡·네이버 앱 등에서 열었을 때** 설치가 불가능하다는 사실과
  기본 브라우저로 나가는 방법을 안내한다. Android 는 크롬을 바로 연다.
- **목록에서 `−한 잔`** 으로 30ml 를 바로 깎을 수 있다. 잘못 눌렀으면 토스트에서 취소.
- **검색**이 관련도 순으로 정렬된다(완전일치 → 이름 시작 → 이름 포함 → 증류소 → 그 외).
  타자 중에는 목록을 갱신하지 않아 입력이 밀리지 않는다.
- **시음 기록이 타임라인**으로 바뀌었다. 직전 기록 대비 점수 변화와
  '개봉 N일 후' 를 함께 보여 준다.
- 검색 결과가 없을 때 **무엇이 걸러내고 있는지** 보여 주고 한 번에 해제한다.
- 이미지 출처를 위키미디어 파일 페이지로 **링크**하고 라이선스를 밝힌다.
- iOS 실행 화면(스플래시)을 넣어 열 때 흰 화면이 보이지 않게 했다.
- 작은 글씨 색 대비를 3.02 → 4.91 로 올렸다(WCAG AA).
- 버튼을 누를 수 있는 범위를 넓혔다(40px 미만 15개 → 0개).
- 헤더에 **버전 표시**, 푸터에 **변경 이력**을 추가했다.

---

## v1.0.0 — 2026-08-01

첫 공개.

- 위스키 이름만 입력하면 타입·증류소·캐스크·테이스팅 노트·평점 **초안**이 채워진다.
  한글 표기로도 찾는다(발음 골격 매칭).
- 설치형 앱(PWA) — Android·iPhone·PC. 오프라인 동작, 저장소 보호.
- 사진: 위키미디어 자동 검색 · 직접 촬영 · URL. 병당 5장, 3:4 자동 맞춤.
- 상태 관리(미개봉·오픈·비움·경험·위시)와 잔량, 시음 기록.
- 주종 4탭(위스키·와인·리큐르·기타주류), 필터·정렬·리스트 보기.
- 통계 — 보유 현황, 주종·타입 분포, 구매 합계, 시세 대비 평가 손익.
- 기기 간 동기화(선택, 본인의 GitHub 비공개 저장소) · JSON·CSV 백업.
- 전체 초기화 4중 안전장치와 되돌리기.
- 사용 가이드 · GitHub 연동 가이드 · 개인정보처리방침.
