# 개선 계획 — 전체 · 구매 · 경험 · 위시 탭 분리와 탭별 통계

작성 2026-08-07. 요청: **전체 / 구매 / 경험 / 위시를 탭으로 분리해 관리하고, 통계도 각 탭에
맞게** 나오도록. 같은 날 사용자와 설계 검토를 거쳐 다섯 가지를 확정했다(§1).
4단계로 나눈다. 단계마다 커밋하고, 넘어가기 전에 브라우저에서 **수치로** 확인한다
(눈으로 보지 않는다 — 실록 V장).

## 용어 — 탭 하나가 술장 하나다

사용자의 말: **탭이 하나의 술장**이다. 전체 술장 · 구매 술장 · 경험 술장 · 위시 술장.
카드는 **자기 내부 설정(상태)에 따라** 자기 술장에 들어가고, 상태를 바꾸면 술장을 옮긴다.
그래서 축이 하나다 — 병의 상태가 곧 술장이라 **별도 필드도 이동 UI도 필요 없다.**
통계는 **술장마다** 낸다. 술장마다 물어볼 것이 다르기 때문이다:
구매 술장은 "얼마 썼고 얼마 남았나", 위시 술장은 "다 사면 얼마인가",
경험 술장은 "그중에 뭘 살까".

---

## 0. 먼저 확인한 것 — 절반은 이미 되어 있고, 나머지 절반은 전혀 안 되어 있다

코드를 읽고 나온 사실이 이 계획의 형태를 정한다.

**분류는 이미 있다.** `STATUS = ['미개봉','오픈','비움','경험','위시']` 이고
`OWNED = ['미개봉','오픈','비움']` 이다. `visible()` 의 `V.status` 는 이미
`'all'` · `'__owned'`(보유만) · `'경험'` · `'위시'` 를 처리한다.
**즉 네 탭은 새로운 데이터도 새로운 필터 로직도 필요 없다** — 필터 시트 안에 묻혀 있는
축 하나를 최상위로 끌어올리는 일이다. 데이터 스키마는 손대지 않으므로 동기화·마이그레이션
문제도 없다.

**통계는 탭을 전혀 모른다.** `openStats()` 는 첫 줄이 `const a = DB.items` 다. 열두 개
KPI, 연도별 구매, 주종별, 맛 프로필 — **전부 컬렉션 전체**로 계산한다. 현재 화면에서
무엇을 보고 있든 같은 숫자가 나온다. 요청의 무게는 여기에 있다.

**지나가다 찾은 실제 버그.** 사이드바 요약(`#sideStats`)이
`보유 <b>${DB.items.length}</b>` 를 쓴다. `DB.items.length` 는 **경험·위시까지 포함한
전체 개수**인데 라벨은 '보유'다. 같은 앱의 `openStats()` 는 `OWNED.includes(i.status)` 로
거른다 — **두 곳이 다른 수를 보여 준다.** 3단계에서 함께 고친다.

---

## 1. 확정된 결정 (2026-08-07 검토)

| # | 결정 | 이유 |
| :--- | :--- | :--- |
| 1 | **`구매` 탭 = `OWNED` 그대로** (미개봉 + 오픈 + **비움**) | 탭의 정의를 "지금 갖고 있는 것"이 아니라 **"내가 산 모든 병"** 으로 둔다. 구매액·연도별 구매 집계가 자연스럽게 맞아떨어지고, 다 마신 병도 컬렉션 이력으로 남는다 |
| 2 | **탭이 `#catSegs` 자리를 쓰고, 주종은 칩 행 앞으로** | 줄 수가 늘지 않고 주종 전환도 한 번에 닿는다 |
| 3 | **경험·위시 통계는 전용 지표만 가볍게** | 그 탭의 병 수가 적으면 KPI 격자는 허상이다. 병이 늘면 그때 더한다 |
| 4 | **통계는 현재 술장 기준** | 보고 있는 것의 통계가 나오는 것이 자연스럽다. 전체 술장 탭이 곧 `전체로 보기` 이므로 별도 토글은 두지 않는다 |
| 5 | ~~술장 상단에 한 줄 요약, 탭하면 펼침~~ → **철회(2026-08-08)** | 3단계에서 넣었다가 **없앴다.** 아래 참고 |

> **결정 5 철회 (2026-08-08).** 실기에서 142병으로 써 본 뒤 사용자가 상단 정리를 요청했다.
> 요약 줄은 접혀도 39px + 여백을 먹었고, 상단 다섯 행이 220px 이라 폰에서 첫 카드까지
> 347px(뷰포트의 43%)이었다 — 그리드에서 카드가 1.4개만 보였다.
> **요약 바를 삭제하고 상단을 3행으로 정리했다**(별도 요청서, 커밋 참조).
> 통계는 상단 `통계` 버튼(시트)과 데스크톱 사이드바에만 남는다 — 사이드바는 세로 공간을
> 먹지 않으므로 유지한다. `statsFor()`/`statsLine()` 은 그대로 쓰인다.
> `wc.stats`(펼침 상태)는 없어졌다.
> **교훈** 목록 위에 상주하는 요약은 "한 줄"이어도 목록 화면에서는 비싸다.
> 39px 은 작아 보였지만 실제 비용은 그 줄 하나가 아니라 **다섯 번째 행이라는 사실**이었다.

### 결정 1이 만드는 숙제 — 라벨을 고쳐야 한다

`구매` 탭에 비움이 들어가므로 **`openStats()` 의 `보유 병수` KPI 이름이 틀리게 된다**
(다 마신 병을 보유로 세고 있다 — 지금도 그렇다). 고치는 방식:

- KPI 제목 `보유 병수` → **`구매 병수`**. 대상은 그대로 `OWNED`.
- 부제에 이미 `미개봉 n · 오픈 n · 비움 n` 분해가 있다. 여기에
  **`지금 보유 n병`**(미개봉 + 오픈)을 한 줄 더 붙인다. KPI 타일을 새로 만들지 않고
  두 수를 같이 읽게 하는 것이 목적이다.
- 사이드바 요약의 `보유` 도 같은 정의로 맞춘다(§3 의 `statsFor()` 를 공유하므로 자동).
- 목록에서도 "지금 술장에 있는 것만" 볼 수 있어야 한다 → 필터의 `보유만` 을
  **`지금 보유`**(미개봉 + 오픈)로 재정의한다. 근거는 §4.

---

## 2. 화면 — 탭이 주종 자리를 쓴다

폰의 현재 세로 순서:

```
header.top        워드마크 · 버전 · 통계 · 설정 · ＋추가
#catSegs          주종 세그먼트  ← 여기가 탭 행이 된다
.search           검색
#typeChips        타입 칩        ← 주종 칩이 이 행 앞으로 들어온다
.rowf             필터 · 정렬 · 뷰 토글
#activeChips      적용된 필터
```

첫 카드까지 이미 다섯~여섯 줄이다. **일곱 번째 줄을 만들지 않는다** — 카드 한 줄을
아끼려고 장소 칩을 반폭 5개 한 줄로, 노트 아이콘을 10열 한 줄로 조정해 온 화면이다.

축의 성격이 배치를 정한다. 탭은 **"이 병이 내 것인가"**, 주종·타입은
**"이 병이 무엇인가"** 다. 뒤의 둘은 같은 질문이라 한 행에 모아도 읽힌다.
주종은 4종뿐이므로(위스키·와인·리큐르·기타주류) 칩 3~4개 + 타입 칩이 가로 스크롤
한 행에 들어간다.

**데스크톱(1024px+)** 은 `#catSegs` 가 `display:none` 이고 사이드바가 주종을 담당한다.
사이드바에는 `navsec` 섹션 구조가 이미 있으니 **`탭` 섹션을 하나 더** 둔다. 공간이 남는
쪽이라 문제되지 않는다.

### 칩의 숫자는 무엇을 세는가

지금 `#catSegs` 의 숫자는 `DB.items` 전체를, `#typeChips` 의 숫자는 선택된 주종
안(`inCat`)을 센다. **부모 축까지만 반영한다**는 규칙이다. 이걸 유지한다:

- **탭 배지** = 컬렉션 전체 기준. 검색어·필터에 따라 흔들리지 않는다
  (타자를 칠 때마다 탭 숫자가 변하면 읽을 수 없다).
- **주종 칩** = 현재 탭 안 기준. → 탭을 바꾸면 주종 숫자가 달라진다. 의도된 것이다.
- **타입 칩** = 현재 탭 + 주종 안 기준.

### 탭을 바꿀 때 유지되는 것

**정렬과 검색어는 유지한다.** 탭마다 정렬을 따로 기억하면 왜 순서가 바뀌었는지 알 수 없다.
검색어도 유지한다 — 다만 그 결과가 0건이면 빈 결과 안내에 **탭도 이유로 나열**해야 한다
(§4).

---

## 3. 통계 — 탭마다 무엇을 보여 주는가

`전체` 와 `구매` 는 **지금의 KPI 집합 전체**를 쓴다. 대상 집합만 달라진다
(`구매` 는 경험·위시를 빼므로 평균 평점·평균 도수·증류소 다양성이 전체와 달라진다 —
그것이 탭을 나눈 값이다).

`경험` 과 `위시` 는 **KPI 격자를 만들지 않는다.** 구매가·잔량·개봉일이 없어서 열두 KPI 중
여덟이 `0원` 과 `—` 로 채워지고, 그 화면은 "통계가 고장났다"로 읽힌다. 대신 그 탭에서만
의미가 있는 지표를 소수로 둔다.

| 탭 | 통계 내용 |
| :--- | :--- |
| **전체** | 지금과 동일 (KPI 12 + 연도별 구매 + 주종별 + 맛 프로필 + 주종별 지출) |
| **구매** | 전체와 같은 구성, 대상만 `OWNED`. `구매 병수` 부제에 `지금 보유 n병` 추가 |
| **경험** | 병 수 · **`구매 후보`**(아래) · **마셔 본 곳** 분포 |
| **위시** | 병 수 · **`예상 지출`**(아래) |

### 탭 전용 지표 — 걸러 낸 숫자가 아니라 그 탭에만 있는 질문

**경험 — `구매 후보`.** 마셔 봤는데 아직 사지 않은 병을 평점 높은 순으로.
경험 기록의 존재 이유가 바로 이것이다("그거 좋았는데 뭐였지"). `bestScore()` 와
상태 `경험` 으로 정렬한다. 곁들여 **마셔 본 곳** — 시음 기록의 `place`
(집·바·모임·회식·기타) 집계.

> ⚠ **이미 산 것이 후보에 계속 남는 함정.** 마셔 본 병을 나중에 사면 이 앱은 기록을
> 두 개로 만든다 — 초안 엔진이 내 컬렉션을 검색원으로 쓰면서도 구매 정보는 가져오지 않고
> **새 병**으로 취급하기 때문이다(`draftFrom` 주변의 주석). 그러면 옛 `경험` 항목이
> 그대로 남아 **이미 산 술을 계속 "사라"고 추천한다.**
> 조치: `구매 후보` 는 **같은 이름의 구매 항목이 있으면 제외한다.** 새로 만들 것은 없다 —
> `dupCandidates(q, exclude)` 가 이미 `squish()` 로 이름을 정규화해 매칭하고
> `owned`(=`OWNED`)와 `tried`(=`경험`)를 갈라 놓는다. 그걸 그대로 쓴다.
> 제외된 개수는 조용히 버리지 말고 `이미 구입 n병 제외` 로 적는다.

**위시 — `예상 지출`.** 위시 전체의 시장가 중간값 합계와 최고가 병.
"위시를 다 사면 얼마인가"는 위시 목록에만 있는 질문이다. `marketMid()` 가 이미 있으므로
합산만 하면 된다.

### 구현 형태

`openStats()` 는 지금 150줄짜리 하나의 템플릿 리터럴이고 `DB.items` 를 직접 참조한다.
**`statsFor(items, tab)` 로 뽑아낸다.** 대상 집합과 탭을 받아 KPI·막대 조각을 돌려주고,
`openStats()` 는 얇은 호출자가 된다. 이렇게 해야 `#sideStats` 도 같은 함수를 써서
사이드바 요약과 통계 시트가 어긋나지 않는다 — 지금 어긋나 있는 것이 §0 의 그 버그다.

### 어디에 보여 주는가 — 목록 위 한 줄 (결정 5)

술장을 열면 목록 **바로 위**에 그 술장의 요약이 한 줄로 있다. 누르면 펼쳐진다.

```
구매 술장   127병 · 지금 보유 118 · 구매 1,840만 · 평균 잔량 62%    ⌃
경험 술장   14병 · 구매 후보 3                                      ⌃
위시 술장   8병 · 예상 지출 96만                                    ⌃
```

- **한 줄에 담는 것**: 병 수 + 그 술장에서 가장 먼저 볼 수치 2~3개.
- 펼치면 §3 표의 내용이 나온다. **펼친 상태를 기억한다**(`wc.stats`) — 매번 다시
  펼치게 하면 펼치는 기능 자체를 안 쓴다.
- 상단 `통계` 버튼(시트)은 남긴다. 같은 `statsFor()` 를 쓰되 **현재 술장 기준**으로
  열린다(결정 4). 한 줄 요약은 흘려보는 자리, 시트는 들여다보는 자리다.
- 요약 줄은 **폰과 데스크톱 모두** 목록 위에 둔다. 데스크톱 사이드바 하단
  (`#sideStats`)도 같은 함수를 쓰므로 세 곳의 숫자가 어긋날 수 없다.

---

## 4. 필터·정렬과 겹치는 문제

탭이 상태 축을 가져가면 필터 시트의 `상태` 와 **두 갈래로 같은 일**을 하게 되고,
서로 모순되는 조합(탭 `위시` + 상태 `미개봉` → 항상 0건)이 만들어진다. 정리한다.

| 항목 | 조치 |
| :--- | :--- |
| `__owned`(보유만) | **삭제하지 않고 재정의한다** — 대상을 `OWNED` 에서 **미개봉 + 오픈**(비움 제외)으로 좁히고 라벨을 `지금 보유` 로 바꾼다. 옛 의미(`OWNED` 전체)는 구매 탭과 중복이라 지우는 것이 맞지만, **새 의미는 중복이 아니다** — 결정 1 로 비움이 구매 탭에 들어오면서 "지금 술장에 있는 것만" 보는 길이 사라진다. 상태 필터는 **단일 선택**이라 `미개봉` 과 `오픈` 을 함께 고를 수 없으므로, 이 항목이 없으면 그 조합을 만들 방법이 아예 없다 |
| `경험` · `위시` | **삭제.** 탭으로 올라간다 |
| `미개봉` · `오픈` · `비움` | 남긴다. **구매·전체 탭에서만** 보여 준다 |
| `__todo`(기록 미완성) · `__stale`(개봉 90일+) | 남긴다. 보유한 병에만 의미가 있으므로 구매·전체 탭에서만 |
| `__draft`(초안 확인) | 남긴다. 모든 탭에서 의미가 있다 |
| 정렬 `잔량순` · `개봉일순` | 경험·위시 탭에서 숨긴다. 값이 없어 전부 동순위가 된다 |
| 정렬 `가성비순` | 구매가가 필요하다. 경험·위시에서 숨긴다 |

빈 결과 안내(`#emptyBox`)는 지금 무엇이 걸러내고 있는지 나열한다. **탭도 그 목록에
넣어야 한다** — 안 넣으면 위시 탭에서 "조건에 맞는 병이 없습니다"만 보이고 이유를 알 수 없다.

---

## 5. 단계

각 단계 끝에 커밋한다. 푸시는 사용자가 한다.

### 1단계 — 탭 자체 (통계는 그대로 둔다)

- `TABS = [['all','전체'],['own','구매'],['exp','경험'],['wish','위시']]`, `V.tab` 추가.
  `own` 의 대상은 `OWNED`(비움 포함 — 결정 1).
- `visible()` 맨 앞에 탭 절 하나. `V.status` 와 **함께** 걸리게 두고 대체하지 않는다.
- 폰 `#catSegs` → 탭 행으로, 주종은 칩 행 앞으로. 데스크톱 사이드바에 `탭` navsec 추가.
- 두 사본은 **한 함수로 그리고 각 컨테이너에 핸들러를 따로 붙인다.** 문서 위임이
  아니다 — 이 앱의 규칙이고, 세 번째 사본을 만들면 배선이 또 필요하다.
- `V.tab` 을 `wc.tab` 에 저장한다(`wc.view` 와 같은 방식). 켤 때 마지막 탭으로 돌아온다.

**확인** — 탭별 개수가 상태별 실제 개수와 맞는지, 360px 에서 탭 행·칩 행이 넘치지
않는지(`scrollWidth > clientWidth`), 카드 4줄·높이 편차 0 이 유지되는지.

### 2단계 — `statsFor()` 추출 (**숫자가 하나도 바뀌지 않아야 한다**)

순수 리팩터. 검증 방법을 미리 정한다:

1. 바꾸기 전에 통계 시트를 열고 **렌더된 텍스트를 그대로 떠 둔다**
   (`$('#shBody').innerText`).
2. 리팩터 후 전체 탭에서 다시 떠서 **문자열이 같은지** 비교한다.
3. 다르면 그건 리팩터 실수다 — 이 단계에서는 숫자가 바뀔 이유가 없다.

같은 데이터로 비교해야 하므로 이 두 측정 사이에 컬렉션을 건드리지 않는다.

### 3단계 — 술장별 통계와 목록 위 요약 줄

§3 을 구현한다. `전체`·`구매` 는 같은 구성에 대상만 다르게, `경험`·`위시` 는 전용 지표만.
목록 위 **요약 줄 + 펼침**(결정 5, `wc.stats` 에 펼침 상태 저장),
`구매 병수` 라벨과 `지금 보유 n병` 부제(결정 1의 숙제), `#sideStats` 를 `statsFor()` 로
갈아 끼우고 **'보유' 라벨 버그를 고친다.**

요약 줄은 새 클래스가 필요하다 — **추가 전에 `grep`** (실록 C-1).
접힌 높이를 재서 첫 카드가 얼마나 밀리는지 360px 에서 확인한다. 한 줄이 40px 을
넘으면 한 줄이 아니다.

**확인** — 상태를 하나씩 만들어 네 탭을 각각 열어 본다(미개봉·오픈·비움·경험·위시를
전부 한 개씩 — 실록 V장의 "상태 분기는 모든 값을 하나씩 만들어 확인한다").
경험·위시 탭에 `—` 로만 채워진 지표가 남아 있지 않은지 확인.

### 4단계 — 필터·정렬·빈 결과 정리

§4 의 표를 구현한다. 마지막에 `CHANGELOG.md` 와 앱 안 `CHANGES` 를 맞추고,
셸 버전 `V` 를 올린다.

---

## 지키는 것

- **카드 레이아웃을 건드리지 않는다.** 4줄 고정과 `.cfoot` 폭 예산은 실측으로 잡았다
  (실록 C-3 · C-4). 탭을 나눈다고 카드에 상태 배지를 더 넣지 않는다 —
  `.cmark` 가 이미 사진 위에서 그 일을 한다.
- **새 클래스 이름은 추가 전에 `grep`.** 스타일시트가 하나뿐이라 이름 충돌이 실제 사고로
  이어진다(실록 C-1 — `.side` 가 편집 폼을 가렸다).
- 무의존 유지. 차트 라이브러리를 넣지 않는다. 막대는 지금처럼 `div` 로 그린다.
- 데이터 스키마·`THUMB_FIT`·동기화 계약은 **손대지 않는다.** 이 작업은 표시 계층이다.
- 탭과 무관하게 **전체 컬렉션 기준으로 남겨야 하는 것**: 설정의 저장 용량·동기화,
  중복 구매 경고, 초안 검색원(내 컬렉션). 탭으로 좁히면 오답이 된다.
- 좁은 폭은 캡처하지 말고 잰다(실록 V-4). 폰 자판이 걸린 화면은 `--vvb`/`--vvh` 를
  손으로 넣어 잰다(실록 C-10).
