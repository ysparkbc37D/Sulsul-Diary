# 술술다이어리 앱 배포 계획

안드로이드 / iPhone / PC 에 **설치해서 쓰는 앱**으로 배포하기 위한 계획.
현재 구성(2026-08-02 실측)을 기준으로, 이미 된 것과 남은 것을 구분했다.

---

## 0. 먼저 알아야 할 제약 — 출처(origin)가 데이터를 결정한다

데이터는 `localStorage` 에 저장되고, `localStorage` 는 **출처마다 완전히 분리**된다.
현재 출처는 `https://ysparkbc37d.github.io` 다.

| 배포 방식 | 출처 | 기존 데이터 |
| :--- | :--- | :--- |
| 브라우저 · 홈 화면에 추가 | `ysparkbc37d.github.io` | 그대로 유지 |
| Android TWA (Play 스토어) | 같음 (브라우저 엔진을 그대로 씀) | **그대로 유지** |
| 커스텀 도메인으로 이전 | `sulsul.example.com` | **전부 사라짐** |
| Capacitor 등 파일 번들 앱 | `capacitor://localhost` | **전부 사라짐** |

결론: **TWA 는 안전하고, 도메인 변경과 번들 앱은 데이터 이전 작업이 필수다.**
이전 수단은 이미 있다 — GitHub 동기화 또는 JSON 백업/복원.
도메인을 바꿀 생각이 있다면 **지금, 병이 더 늘기 전에** 바꾸는 편이 낫다.

---

## 1. 현재 상태 (실측)

### 이미 되어 있는 것

- `manifest.webmanifest` — `name` / `short_name` / `id` / `scope` / `start_url` /
  `display:standalone` / `display_override` / `orientation` / `theme_color` /
  `background_color` / `categories` / 아이콘 3종(192, 512, maskable 512)
- `<meta viewport ... viewport-fit=cover>`, `theme-color`(테마 전환 시 갱신)
- iOS 메타: `apple-touch-icon`, `apple-mobile-web-app-capable`,
  `apple-mobile-web-app-status-bar-style`, `apple-mobile-web-app-title`
- `beforeinstallprompt` 를 잡아 두고 설정창의 **홈 화면에 추가** 버튼에서 사용,
  미지원 브라우저에는 안내 토스트로 대체
- 서비스워커 등록 + 셸 캐시(코드·데이터는 network-first, 사진은 IMG/IMGC 분리)
- 외부 의존성 0 — 폰트(Cormorant Garamond)까지 `data:font/woff2` 로 인라인,
  QR 생성기도 자체 구현. **비행기 모드에서도 전부 동작한다.**

즉 **Android 와 PC 는 지금 상태로도 "설치"가 된다.** 주소를 열고 설치만 누르면 된다.

### 빠진 것

| # | 항목 | 영향 | 난이도 |
| :--- | :--- | :--- | :--- |
| A | `navigator.storage.persist()` 미호출 | **데이터 유실 위험** | 낮음 |
| B | `manifest.screenshots` 없음 | Android 설치 UI 가 빈약, 스토어 등록 시 필수 | 낮음 |
| C | 서비스워커 갱신 알림 없음 | 새 버전을 받았는지 알 수 없음 | 낮음 |
| D | iOS 설치 안내가 일반 문구 | iPhone 사용자가 설치법을 못 찾음 | 낮음 |
| E | iOS 스플래시 이미지 없음 | 실행 시 흰 화면이 잠깐 보임 | 중간 |
| F | maskable 192 아이콘 없음 | 일부 런처에서 아이콘 여백 어색 | 낮음 |
| G | `manifest.shortcuts` 없음 | 아이콘 길게 눌러 "병 추가" 같은 바로가기 없음 | 낮음 |

---

## 2. 단계별 계획

### 1단계 — 설치형 PWA 완성 (권장, 이것만으로 세 플랫폼 커버)

작업량이 작고 스토어 심사·비용·계정이 전혀 필요 없다. 위 A~G 를 처리한다.

**A. 저장소 영구화 — 가장 중요**

```js
if (navigator.storage?.persist) await navigator.storage.persist();
```

브라우저는 저장 공간이 부족하면 `localStorage` 를 **임의로 비울 수 있다.**
특히 iOS Safari 는 **설치하지 않은 사이트를 7일간 안 열면 데이터를 지운다.**
`persist()` 를 호출해 승인받으면 그 대상에서 제외된다. 설치된 PWA 는 대체로
자동 승인된다. 설정창에 현재 상태(`navigator.storage.estimate()` 사용량 포함)를
표시해 두면 사용자가 확인할 수 있다.

> 이것과 무관하게 **GitHub 동기화를 켜 두는 것이 실질적인 백업이다.**
> 지금도 켜져 있고 sha 충돌 복구까지 들어갔으므로, 기기가 하나 죽어도 복구된다.

**B. 스크린샷 추가**

```json
"screenshots": [
  { "src": "./shots/phone-1.png", "sizes": "1080x1920", "type": "image/png",
    "form_factor": "narrow", "label": "컬렉션 목록" },
  { "src": "./shots/desktop-1.png", "sizes": "1920x1080", "type": "image/png",
    "form_factor": "wide", "label": "데스크톱 화면" }
]
```

Chrome Android 는 `screenshots` + `description` 이 있을 때만 풍부한 설치 카드를
띄운다. 나중에 스토어에 올릴 때도 같은 이미지를 쓴다.
스크린샷은 이 저장소의 검증 절차(헤드리스 캡처)로 만들 수 있다.

**C. 갱신 알림**

```js
reg.addEventListener('updatefound', …)  // 새 워커 발견 → "새 버전이 있습니다 · 새로고침"
```

지금은 코드가 network-first 라 다음 실행에 자동 반영되지만, 사용자가 그 사실을
모른다. 토스트 한 줄로 충분하다.

**D. iOS 설치 안내**

iPhone 은 설치 프롬프트가 **없다.** 반드시 공유 버튼 → 홈 화면에 추가 를 거쳐야 한다.
iOS 이면서 아직 미설치일 때만 안내를 띄운다.

```js
const isIOS = /iP(hone|ad|od)/.test(navigator.userAgent);
const installed = matchMedia('(display-mode: standalone)').matches
  || navigator.standalone;
```

**E~G** 는 마감 작업. 스플래시는 기기별 크기가 많아 주요 해상도 3~4종만 넣는다.

**예상 작업량:** A·C·D·F·G 는 코드 소량. B·E 는 이미지 생성이 대부분.

---

### 2단계 — Android Play 스토어 (원한다면)

**Trusted Web Activity (TWA)** 로 감싼다. WebView 가 아니라 Chrome 엔진을
그대로 쓰므로 **주소창 없는 전체화면 + 출처 동일 = 데이터 그대로**다.

```
npx @bubblewrap/cli init --manifest https://ysparkbc37d.github.io/whisky-collector/manifest.webmanifest
npx @bubblewrap/cli build
```

필요한 것:

1. **Digital Asset Links** — `https://ysparkbc37d.github.io/.well-known/assetlinks.json`
   에 앱 서명 지문을 올려야 주소창이 사라진다.
   ⚠️ `github.io` 는 사용자 저장소가 `/<repo>/` 하위 경로로 서비스되므로
   **도메인 루트에 파일을 두려면 `ysparkbc37D.github.io` 라는 이름의 별도 저장소가
   필요하다.** 이것이 TWA 의 유일한 실질 장벽이다.
   (커스텀 도메인을 쓰면 깔끔히 해결되지만 → 0장의 데이터 이전 문제)
2. Play Console 개발자 등록 — **$25 1회**
3. 서명 키스토어 보관 (분실하면 업데이트 불가)
4. 스토어 등록 정보 — 아이콘 512, 피처 그래픽 1024×500, 스크린샷, 개인정보처리방침

**개인정보처리방침이 필요하다.** 이 앱은 서버가 없고 데이터가 기기에만 있으므로
"수집 없음"으로 간단히 쓸 수 있다. GitHub 동기화는 사용자 본인의 비공개 저장소를
쓴다는 점을 명시한다.

---

### 3단계 — iPhone

**솔직한 결론: App Store 등록은 권하지 않는다.**

- Apple 심사 지침 4.2(최소 기능)는 웹사이트를 그대로 감싼 앱을 반려한다.
  통과시키려면 푸시·위젯·카메라 연동 같은 **네이티브 기능을 실제로 넣어야 한다.**
- Apple Developer Program **연 $99**
- Capacitor 등으로 번들하면 출처가 바뀌어 **데이터 이전이 필요하다**

따라서 iPhone 은 **홈 화면에 추가(1단계 D)** 로 간다. 설치하면 아이콘·전체화면·
오프라인이 모두 동작하고, 실사용 경험은 네이티브와 거의 같다.
`persist()` 승인을 받으므로 7일 삭제 규칙에서도 벗어난다.

---

### 4단계 — PC

- **Chrome / Edge**: 지금 이미 설치된다. 주소창 오른쪽 설치 아이콘 또는
  메뉴 → 앱 설치. 창 분리, 작업표시줄 고정, 오프라인 실행 모두 동작.
- **Microsoft Store** (선택): PWABuilder 로 패키징 가능.
  개인 개발자 등록 1회 비용. Play 스토어와 달리 PWA 를 정식으로 받아 준다.
- `display_override` 에 `window-controls-overlay` 를 넣으면 제목줄까지 앱 UI 로
  쓸 수 있으나, 지금 헤더 구조로는 이득이 크지 않다.

---

## 3. 권고

| 우선순위 | 내용 | 이유 |
| :--- | :--- | :--- |
| **1** | 1단계 A (`persist()`) | 데이터 유실 방어. 다른 어떤 것보다 먼저 |
| **2** | 1단계 C·D | 사용자가 설치·갱신을 인지할 수 있게 |
| **3** | 1단계 B·F·G | 설치 화면 완성도 |
| 4 | 커스텀 도메인 결정 | 바꿀 거라면 병이 더 늘기 전에 |
| 5 | 2단계 (Play 스토어) | "진짜 앱" 이 필요할 때. assetlinks 경로 문제 해결이 전제 |
| — | 3단계 (App Store) | 권하지 않음 |

**1단계만 하면 세 플랫폼 모두 설치형 앱이 된다.** 스토어는 배포 범위를
넓히려는 목적(가족·지인 공유, 검색 노출)일 때만 의미가 있고, 혼자 쓰거나
링크로 공유하는 수준이라면 1단계에서 멈추는 것이 합리적이다.

---

## 4. 결정이 필요한 사항

1. **커스텀 도메인을 쓸 것인가?** — 쓰면 TWA 가 쉬워지지만 데이터 이전 필요
2. **Play 스토어에 올릴 것인가?** — $25 + 키스토어 관리 + 개인정보처리방침
3. **누구까지 쓸 것인가?** — 혼자면 1단계로 충분. 배포하려면 GitHub 동기화가
   개인 토큰을 요구하는 구조라 그 부분의 안내가 필요하다
