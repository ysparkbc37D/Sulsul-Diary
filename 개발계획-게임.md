# 개선 계획 — 블라인드 테이스팅 게임 (셀프 모드)

작성 2026-08-08. 요청: 메인에 `GAME` 진입점을 두고, 기존 시음 기록 UI를 재활용해
블라인드 테이스팅 게임을 만든다. **셀프 모드부터.**

---

## 0. 먼저 측정한 것 — 기획의 전제가 하나 깨진다

원안은 점수 100점 중 **70점을 "공식 태그와의 일치"** 에 걸고, 공식 태그가 부족하면
AI가 카테고리당 최소 4개까지 확장하라고 한다. 실제 데이터를 재 봤다.

| 데이터 | 실측 |
| :--- | :--- |
| `kb.js` 의 WB 점수 | 제품 229개 중 **220개** 있음 ✅ |
| `kb.js` 의 **제품별 공식 테이스팅 노트** | 229개 중 **3개** ❌ |
| 컬렉션 6병에서 정답 태그를 카테고리당 4개 이상 확보 | **0병** ❌ |

컬렉션의 노트는 초안 엔진이 넣은 **산문**이다(`짙은 피트 스모크, 타르, 레몬 제스트`).
디스크립터 84개와 정확히 일치하는 것은 축당 0~2개뿐이다. 부분 문자열로 늘리면 2~5개,
아이콘 계열로 더 늘려도 **여운이 0개인 병이 나온다**(라가불린 16년).

### 그래서 이렇게 한다

**정답은 AI가 아니라 "내가 기록한 노트"다.** 셀프 모드에서는 이것이 더 맞다 —
게임의 질문이 *"이 위스키의 객관적 노트를 맞혀라"* 에서
***"내가 적어 둔 내 감각을 내가 다시 알아맞히나"*** 로 바뀐다.
서버도 AI도 필요 없고, 판정이 결정적이고, 정답의 출처를 사용자가 안다.

정답 태그 추출은 3단계다(`gameTags`).

1. **정확 일치** — `notePicked()` 가 이미 하는 일. 아이콘 그리드로 고른 항목은 여기서 잡힌다.
2. **부분 문자열** — 산문 안에 디스크립터 이름이 들어 있으면 인정(`짙은 피트 스모크` → `피트`).
3. **아이콘 계열 확장** — 같은 계열을 정답으로 인정(`건포도`·`말린 과일`·`셰리` = `dried`).
   28개 계열 중 23개가 2개 이상을 묶고 있어 동의어·상위개념 매칭이 **AI 없이** 된다.

그래도 부족한 축이 남는다. **부족한 축은 채점에서 빼고 배점을 남은 축에 비례 배분한다.**
0개인 축을 20점 만점으로 채점하면 무엇을 골라도 0점이라 게임이 망가진다.
셋업 화면에 축별 정답 태그 수를 그대로 보여 주고, **모자란 병은 사용자가 노트를 채워
개선할 수 있게** 편집으로 보내는 링크를 둔다(아이콘 그리드로 고르면 1번에서 정확히 잡힌다).

---

## 1. 배점 — 비율식. 정답 개수로 정규화한다

100% 를 **이름 10 · WB 20 · 향 20 · 맛 30 · 여운 20** 으로 나눈다.
정답이 **내 노트**이므로 태그 70% 의 신뢰도 문제가 사라진다(§0).

축 점수는 **정답 개수로 나눈다.**

```
축점수 = 배점 × clamp(0, 1, (일치 − 불일치 ÷ 3) ÷ 정답개수)
```

정답 태그가 10개인 맛(30%)이면 개당 3%, 다 맞히면 30%. 여기에 오답 5개가 더 붙으면
`30 − 5 = 25%`. 증가 : 감소 = **3 : 1** 이다.

이 정규화가 §0 에서 드러난 문제를 정확히 없앤다 — 실측한 정답 태그 수는 축마다
**2~7개로 들쭉날쭉한데**, 개당 고정 점수로 두면 정답이 2개인 축은 만점이 쉽고
7개인 축은 만점이 아예 불가능하다. 개수로 나누면 **몇 개든 다 맞히면 만점**이다.

따라오는 결론 두 개.

- **선택 상한이 필요 없다.** 84개를 다 찍으면 오답 페널티가 정답을 넘어 자동으로
  0점이 된다(실측 확인). 확신 없는 것을 더 찍을 이유가 구조적으로 없으므로
  어뷰징 방지용 상한을 따로 두지 않는다.
- **0 아래로 내려가지 않으므로** 한 축에서 잃는 최대치는 그 축의 배점뿐이다.
  한 축의 실패가 다른 축을 끌어내리지 않는다.

정답 태그가 **0개인 축의 배점은 재분배하지 않는다.** 대신 **셋업에서 막는다** —
축이 빈 병은 `채점 불가` 로 표시하고 노트를 채우도록 편집으로 보낸다.
점수를 몰래 옮기는 것보다 정직하다.

**WB 점수가 없는 병만** 그 20% 를 남은 항목에 비례 배분한다. 값의 유무는 사용자가
고칠 수 없으니(kb.js 소관) 여기서는 배분이 맞다. `kb.js` 값은 `≈87 (참고)` 형태의
**정수 근사치**라 오차 1점까지는 만점으로 두어 근사 오차를 흡수하고, 9점 오차에서
0점이 되도록 선형으로 깎는다.

### 실측 검증 (`scoreGlass`, 맛 30% 기준)

| 정답 개수 | 내 선택 | 결과 | 기대 |
| :--- | :--- | :--- | :--- |
| 10 | 10개 다 맞춤 | **30** | 30 ✅ |
| 10 | 10개 맞춤 + 오답 5 | **25** | 25 ✅ |
| 10 | 5개 맞춤 | **15** | 15 ✅ |
| 10 | 1개 맞춤 + 오답 5 | **0** | 0 ✅ |
| 2 | 2개 다 맞춤 | **30** | 30 ✅ |
| 7 | 7개 다 맞춤 | **30** | 30 ✅ |
| 10 | 84개 전부 찍기 | **0** | 0 ✅ |

---

## 2. 화면

### 진입점

`GAME` 버튼을 2행(검색 옆)에 둔다. 원안은 필터·초기화를 3행으로 내리라고 했지만,
3행은 이미 드롭다운 2개 + 개수 + 보기로 채워져 있어 360px 에서 버튼 3개를 더 넣으면
드롭다운이 93px 로 줄어 `주종 전체 (145)` 가 잘린다. **2행에 GAME 을 더한다** —
검색 폭이 193px 로 남아 쓸 수 있다. 실측하며 맞춘다.

### 게임 목록

`GAMES` 배열로 둔다 — 새 게임을 추가할 때 배열에 항목만 늘리면 된다(확장성 요구).
셀프는 바로 동작하고, 멀티는 목록에 두되 **서버가 필요하다는 안내와 함께 비활성**이다.
할 수 없는 것을 할 수 있는 것처럼 두지 않는다.

### 시음 UI 재활용

`openSession(id)` → `openSession(id, opt)` 로 인자 하나를 더한다.
(원안의 `isBlindGameMode` props 는 React 문법이고 이 앱은 바닐라 템플릿이다.)
`opt.blind` 일 때 숨기는 것: `마신 날` · `장소` · `인상·메모` · `함께한 사람` ·
`잔량 차감`. 더하는 것: **예상 위스키 검색·선택**.

> ⚠ **게임 기록을 `it.sessions` 에 넣지 않는다.** 그 배열은 `rating_my`(최근 점수)와
> 잔량 차감, 술장 통계로 전부 흘러간다. 블라인드 결과는 `wc.game` 에 따로 둔다 —
> 넣으면 컬렉션 통계가 게임 점수로 오염된다.

---

## 3. 데이터

`wc.game` 하나에 진행 중인 게임 한 판을 둔다(끝나면 결과를 남기고 비운다).

```js
{ v:1, mode:'self', at:ISO, n:3,
  glasses:[ { letter:'A', itemId, ans:{wb, tags:{nose:[],taste:[],finish:[]}, miss:['finish']},
              my:{guessId, wb, tags:{...}}, done:false } ],
  revealed:false }
```

**정답은 셋업 때 한 번만 계산해 고정한다**(원안의 Data Locking).
컬렉션을 나중에 고쳐도 진행 중인 게임의 정답이 흔들리지 않는다.

---

## 4. 단계

| 단계 | 내용 | 상태 |
| :--- | :--- | :--- |
| 1 | `GAME` 진입점 + 게임 목록 + 셀프 셋업(병 선택 · 정답 태그 수 표시 · 셔플·고정) | 완료 |
| 2 | 잔별 블라인드 입력 + 예상 위스키 선택 | 완료 |
| 3 | 채점(`scoreGlass`)과 결과 리포트 | 완료 |
| 4 | CHANGELOG · 버전 (`1.5.0` / `wc-shell-v47`) | 완료 |

### 시음 UI 재활용을 어떻게 했나

원안은 `openSession` 에 `isBlindGameMode` 를 넘겨 분기하라고 했다. 그렇게 하지 않았다.
**저장 경로가 완전히 다르기 때문이다** — 시음 기록은 `it.sessions.push()` 로 끝나면서
`rating_my` · 잔량 · 상태를 함께 건드리는데, 블라인드는 그 어느 것도 건드려선 안 된다.
한 함수 안에서 분기하면 그 오염을 막을 구조적 방법이 없다.

대신 **마크업을 공용 헬퍼로 뽑았다.** `radarBlock(p, sub)` 와 `noteSections(target, pre)` 를
시음 기록과 게임이 함께 쓴다(`p` 는 id 접두어, `ss` / `gg`). 화면은 같은 것이 나오고,
저장부는 형제 함수로 갈라져 섞이지 않는다.

실측 확인: 게임 한 판을 끝까지 돌린 뒤 컬렉션은 **시음 기록 0건 · `rating_my` 없음 ·
잔량 100% · 미개봉 유지**였고, `DB.items` 에 게임 키(`my`/`ans`/`letter`)가 새지 않았다.

## 5. WB 예상 점수는 따로 묻지 않는다

처음에는 잔 입력 화면 아래에 `Whiskybase 예상 점수` 숫자 칸을 두었다. 없앴다.
**맨 위 레이더의 `내 점수`(향·맛·여운 평균)가 그 역할을 겸한다.**

같은 것을 두 번 묻는 셈이었다 — 세 축을 매긴 결과가 곧 "이 술을 몇 점으로 보는가"이고,
이 항목의 뜻은 그것을 세계 평점과 견주는 것이다. `radarScore` 가 세 축 평균(0~100)을
내주므로 WB 점수와 단위도 그대로 맞는다.

실측: 잔 A 레이더 80 대 실제 80 → 오차 0 → **20/20**.
잔 B 레이더 73 대 실제 79 → 오차 6 → **8/20** (`20 × (1 − 5/8) = 7.5` → 8).

## 6. 상단 배치 — 3행에 필터·초기화를 넣은 대가

요청안대로 2행은 `검색 + GAME`, 3행은 `주종 ▾ + 타입 ▾ + 필터 + 초기화 + 보기` 로
옮겼다. 360px 에서 3행은 `93 + 93 + 38×3 + 간격 28 = 328` 로 들어가고 가로 스크롤은 없다.
개수 표기(`statLine`)는 없앴다 — 1행 술장 탭이 이미 세어 주고, 그 폭이 있어야 버튼 셋이
이 행에 들어온다.

대가가 하나 있었다. **드롭다운 글자칸이 55px 로 줄어 `주종 전체 (145)`(105px)의 병 수가
잘려 나갔다.** 병 수는 주종을 칩에서 드롭다운으로 바꿀 때 배지를 포기한 대가로 라벨에
살려 둔 것이라, 그것이 잘리면 남는 것이 없다.

| 시도 | 글자칸 | `주종 전체 (145)` |
| :--- | :--- | :--- |
| 요청안 그대로 | 55px | 잘림 |
| 우측 패딩 26 → 16px | 65px | 잘림 |
| 초기화를 2행으로 되돌림 | 77px | 잘림 |

**어떤 배치로도 들어가지 않는다.** 라벨을 줄이는 것 말고는 길이 없었다 —
우측 패딩을 16px 로 줄이고(화살표는 5px 면 된다) 라벨에서 `전체` 와 괄호를 뺐다.
`주종 145` 는 61px 로 65px 칸에 들어간다. 펼치면 전체 라벨이 그대로 보이므로
주종별 병 수는 한 번의 탭 안에 있다.

> ⚠ `sel.scrollWidth > sel.clientWidth` 로 잘림을 재면 **`<select>` 는 늘 "안 잘림" 이라고
> 답한다** — 선택된 옵션을 요소가 알아서 잘라 그리기 때문이다. 숨은 `<span>` 에 같은
> 폰트로 라벨을 넣어 글자 폭을 직접 재야 한다. 술술실록 V-10.

## 7. 셀프 테스트 (1병) — 목록의 첫 게임

블라인드는 최소 2병이 필요하고 잔을 두 개 준비해야 한다. **1병 모드는 지금 마시고
있는 한 병으로 바로 된다** — 진입 장벽이 가장 낮은 것이 목록 첫 줄이다.

병을 알고 시작하므로 **`이름` 10% 가 뜻이 없다.** 맞힐 것이 없는 항목을 0점으로
남겨 두면 총점이 영영 90점을 넘지 못하므로, 그 10% 를 세 축에 고루 나눈다
(`gameAlloc('solo')` → 각 `+3.33`). WB 는 남긴다 — 병을 알아도 "세계가 이 술을
몇 점으로 보는가" 는 여전히 맞히는 문제다.

| 항목 | 블라인드 | 셀프 테스트 |
| :--- | :--- | :--- |
| 이름 | 10 | **없음** (항목 자체를 만들지 않는다) |
| WB | 20 | 20 |
| 향 / 맛 / 여운 | 20 / 30 / 20 | **23.3 / 33.3 / 23.3** |
| 합 | 100 | 100 (실측 확인) |

블라인드와 다른 곳은 네 군데뿐이다 — 제목에 병 이름을 쓴다 · `어느 병일까요` 를 묻지
않는다 · 잔 목록을 거치지 않고 바로 입력으로 간다 · 저장하면 곧바로 채점된다.

## 8. 정답 개수를 알려 준다 (알려 주지 않으면 판단이 불가능하다)

축 머리에 `정답 7개` 와 `4 고름` 을 나란히 적는다. 이것은 편의가 아니라 **필수**다 —
§1 에서 배점을 정답 개수로 정규화했으므로, 개수를 모르면 몇 개를 골라야 하는지
판단할 방법이 아예 없다. 없던 동안은 결함이었다.

**몇 개인지만 알려 주고 무엇인지는 알려 주지 않는다.** 블라인드에서 이 수가 약한
지문이 될 수는 있으나(내 노트를 외우고 있다면), 정규화 배점을 불투명하게 두는 대가가
그보다 크다.

## 9. 게임 결과 → 시음 기록

`it.sessions` 를 건드리지 않는다는 §2 의 규칙은 **자동 유입에 대한 것**이다.
셀프 테스트는 블라인드가 아니라 병을 알고 실제로 마신 것이므로 진짜 시음이고,
그 결과를 기록으로 남기고 싶은 것이 자연스럽다.

그래서 **사용자가 눌렀을 때만**, 그리고 **저장 로직을 새로 만들지 않고** 처리한다 —
`openSession(id, {rad, tags})` 로 기존 시음 기록 시트를 **채워서 연다.** 잔량 차감 ·
평점 반영 · 미개봉 해제가 전부 그 함수의 저장부에 있으므로, 저장 경로를 하나로 두면
두 갈래가 서로 어긋날 수 없다. 사용자는 거기서 장소·메모를 더하고 저장한다.

실측: 버튼을 누르면 슬라이더 83 · 태그 8개 켜짐 · 메모 `향: 바닐라, 오크, 사과, 꽃 …`
까지 채워지고 **그 시점에는 아직 아무것도 저장되지 않는다**(시음 기록 0건).
저장을 누르면 기록 1건 · 평점 83 · 미개봉→오픈 · 잔량 100→96(30ml) — 기존 동작 그대로다.

## 10. 난이도 — 새 채점식 없이, 있는 레버 네 개로

84개 중에서 아무 힌트 없이 골라내는 것은 처음 하는 사람에게 너무 가혹하다.
그래서 셋업 맨 위에 **초급 · 중급 · 고급** 을 두었다. 새 채점식을 만들지 않고
**이미 있는 기계장치 네 개**를 켜고 끄는 것으로 만든다.

| 레버 | 하는 일 | 초급 | 중급 | 고급 |
| :--- | :--- | :--- | :--- | :--- |
| `showCount` | 축마다 정답이 몇 개인지 알려 주는가 | ✅ | ✅ | ✗ |
| `narrow` | 정답 + 오답 N개만 후보로 보여 주는가 | **6** | 없음 | 없음 |
| `penDiv` | 오답 감점 (정답 하나의 `1/penDiv`) | **없음** | 1/3 | **1/2** |
| `fam` | 같은 아이콘 계열을 정답으로 인정하는가 | ✅ | ✅ | ✗ |

`fam` 이 특히 값싸고 효과가 크다 — §0 의 3단계 추출에서 **3단계만 건너뛰면** 정답이
좁아진다. 글렌피딕 12년으로 재 보면 향 `7 → 5` · 맛 `4 → 3` · 여운 `3 → 2` 다.

### 실측 — 같은 답안을 난이도별로 채점

정답의 절반을 맞히고 오답 2개를 낸 **같은 답안**을 세 난이도로 채점했다.

| 난이도 | 향 | 맛 | 여운 | 총점 |
| :--- | :--- | :--- | :--- | :--- |
| 초급 | 13.3 / 23.3 | 16.7 / 33.3 | 15.6 / 23.3 | **66** |
| 중급 | 11.1 / 23.3 | 11.1 / 33.3 | 10.4 / 23.3 | **53** |
| 고급 | 9.3 / 23.3 | 11.1 / 33.3 | **0** / 23.3 | **40** |

단조롭게 벌어진다. 고급의 여운이 0인 것은 정답이 2개로 좁아진 상태에서 오답 2개를
`1/2` 로 깎아 `1 − 1 = 0` 이 되었기 때문이다 — 의도한 동작이다.

### 고정과 재추첨

난이도(`opt`)와 좁힌 후보(`cand`)를 **판을 만들 때 고정한다** — 정답을 고정하는 것과
같은 이유다. 도중에 난이도가 바뀌어 채점 기준이 흔들리거나, 잔을 다시 열 때 후보가
재추첨되어 **앞서 고른 것이 목록에서 사라지는** 일이 없어야 한다.
`gameCand` 는 정답 전부 + 오답 N개를 담고 **전체를 한 번 더 섞는다** — 정답이 앞에
몰려 있으면 세지 않아도 보인다(실측으로 섞임 확인).

셋업에서 난이도를 바꾸면 **화면을 다시 그리고 고른 병을 버린다.** `fam` 이 정답 범위를
바꾸므로 고를 수 있는 병 목록도 난이도마다 다르고, 고급에서 못 쓰게 된 병이 선택된 채
남으면 시작할 수 없는 판이 만들어진다. 마지막에 고른 난이도는 `wc.glv` 에 기억한다.

> 이 설계가 앞서 열려 있던 질문 하나를 닫는다 — "블라인드에서 정답 개수를 보여 주면
> 약한 지문이 되지 않나". **내가 정할 문제가 아니라 난이도가 정할 문제였다.**

### 직접 설정

네 번째 세그먼트 `직접` 은 레버 넷을 그대로 노출한다. 프리셋 셋으로 안 맞는 조합이
있고(후보는 좁히되 감점은 남기고 싶다), 무엇이 난이도를 만드는지 눈으로 보게 하는
값도 있다. 고른 값은 `wc.gopt` 에 남아 다음 판에도 이어진다.

레버를 만지면 **셋업을 다시 그린다** — 어느 것이든 채점 기준을 바꾸고, `fam` 은
쓸 수 있는 병 목록까지 바꾼다. 요약 한 줄(`optDesc`)이 지금 조합을 읽어 주므로
이름이 규칙을 설명해 주지 않아도 된다.

실측: `fam` 을 끄면 요약이 `정확히 일치만` 으로 바뀌고 글렌피딕의 태그가
`향 7 · 맛 4 · 여운 3` → `향 5 · 맛 3 · 여운 2` 로 즉시 줄었고, 값이 `wc.gopt` 에 남았다.

## 11. 맞힐 항목 — 켜고 끄면 배점이 스스로 나뉜다

블라인드 테이스팅 앱·와인 게임을 조사해 **이미 있는 필드로 채점할 수 있는 항목 셋**을
더했다. 새 데이터를 요구하지 않는다.

| 항목 | 출처가 된 게임 | 쓰는 필드 | 채점 |
| :--- | :--- | :--- | :--- |
| 도수 | [BlindSip](https://blindsip.app/) 의 `Proof Guessing` | `it.abv` | 거리 (±0.5% 만점, 6% 0점) |
| 숙성연수 | 와인 블라인드의 `vintage by distance` | `it.age` | 거리 (±1년 만점, 10년 0점) |
| 지역 | 와인 블라인드의 `region bonus` | `it.region` | 정확히 일치 |

`GAME_CATS` 의 `w` 는 **절대 점수가 아니라 비율의 재료**다. `gameAlloc` 이 켜진 항목의
가중치 합이 100 이 되게 비례 배분하므로, **항목을 켜고 끌 때마다 표를 고쳐 쓰지 않아도
총점이 늘 100 이다.** 기본값(도수·숙성·지역 꺼짐)의 합이 정확히 100 이라
**기존 배점이 한 자리도 바뀌지 않는다.**

### 실측 — 모든 조합의 합이 100

| 조합 | 배점 |
| :--- | :--- |
| 기본 · 블라인드 | 이름 10 · WB 20 · 향 20 · 맛 30 · 여운 20 (**예전과 동일**) |
| 기본 · 셀프 테스트 | WB 20 · 향 23.3 · 맛 33.3 · 여운 23.3 (**예전과 동일**) |
| 전부 켬 · 블라인드 | 8.3 / 16.7 / 8.3 / 4.2 / 4.2 / 16.7 / 25 / 16.7 |
| 향·맛·여운만 | 28.6 / 42.9 / 28.6 |
| NAS 병 (숙성 없음) | 숙성연수가 빠지고 나머지가 8.7 / 17.4 / 8.7 / 4.3 / … 로 재배분 |

**`dead` 를 `miss` 와 섞지 않는 것이 중요하다.** `miss` 는 향·맛·여운 축이 비었다는
뜻이고 셋업이 그 병을 잠그는 근거다. 값이 없는 항목(NAS · WB 없음 · 지역 없음)은
`dead` 에 담아 배점만 흘려보낸다 — 처음에 한 배열에 담았더니 **숙성 미표기라는 이유로
병이 잠겼다.**

이 병에서 실제로 채점되는 항목만 묻는다(`ask`). 껐거나 데이터가 없는 항목을 물으면
답해도 점수가 없어 속은 기분이 된다.

## 12. 힌트 — 값을 따로 정하지 않는다

[BlindVine](https://getblindvine.com/pages/how-to-play) 은 퀴즈를 맞히면 힌트를 준다.
여기서는 축 머리의 `힌트` 가 **아직 안 고른 정답 하나를 켜 준다.** 대가는 하나다 —
**그 하나는 일치 수에서 뺀다.**

```
frac = (일치 − 힌트 − 오답/penDiv) ÷ 정답개수
```

정보는 주되 점수는 주지 않으므로 **힌트 값을 따로 정할 필요가 없다.** 막혔을 때
포기하지 않게 하면서 스스로 균형이 잡힌다. 좁힌 후보 밖의 답일 수 있으니 힌트를 쓰면
그 축의 목록을 다시 그려 넣는다.

실측: 향의 정답 7개 중 힌트로 2개를 받고 그 둘만 켠 상태 →
`일치 2/7 · 힌트 2` → **0점**. 손으로 다 고른 맛·여운은 만점.

## 13. 잔 20개까지

`GAME_LETTERS` 를 `A~T` 20자로 늘렸다. 동호회 시음회는 열 잔을 넘기므로 6잔은 좁았다.

**실제 상한은 `gameMaxG(usable)` 가 정한다** — 20개를 허용해도 채점할 수 있는 병이
8개면 8개가 상한이다. 고를 수 없는 수를 안내에 적으면 거짓말이 된다.
안내문도 사실을 말한다: `2~20개 고르세요 (채점 가능한 29개 중, 한 판 최대 20개)`.

실측: 노트가 꽉 찬 병 24개를 더해 29개로 만든 뒤 순서대로 눌렀더니 20번째까지 켜지고
21번째부터 20에서 멈췄다.

## 14. 멀티 — 서버 없는 핫시트

**서버가 필요 없다.** 기기 하나를 돌려 쓰는 방식이라 물리적 시음회의 흐름과 같다.
`GAMES` 목록에서 잠금을 풀었다.

- **참가자 2~10명.** 이름은 `참가자 1…N` 로 자동이다.
- **잔이 바깥 루프다.** 잔 A 를 참가자 1~N 이 차례로 평가하고, 다 돌면 잔 B 로 간다.
  실제 시음회가 그렇게 움직이기 때문이다 — 모두가 A 를 맛본 뒤 함께 B 로 간다.
  참가자를 바깥 루프로 두면 한 사람이 전부 마시고 넘겨야 한다.
- **답안은 `g.mys[pi]` 다.** 혼자 하는 모드도 참가자 한 명인 배열로 통일했다.
  두 모양(`my` / `mys`)을 쓰면 채점부가 분기되어 한쪽만 고쳐진다.
  옛 판(v3)은 `loadGame` 에서 버린다 — 한 판은 한 자리에서 끝나는 것이라 이어 붙일
  값이 없고, 모양이 달라 억지로 읽으면 조용히 0점이 된다.

### 넘겨주기 화면이 공정성의 핵심이다

기기를 넘기므로 **앞 사람의 입력이 화면에 남은 채로 넘기면 게임이 무너진다.**
차례 사이에 `openGameHandoff()` 를 끼워 한 번 끊고, 다음 사람이 스스로 시작을 누른다.
차례마다 빈 양식으로 시작한다.

실측: 3잔 × 3명 9차례를 끝까지 돌리며 매 차례 시작 시점의 켜진 태그와 예상 병을
검사했다 — **유출 0건.** 차례 순서도
`잔A-P1 · 잔A-P2 · 잔A-P3 · 잔B-P1 …` 로 의도대로 나왔다.

### 결과는 순위표

서버 없이도 순위는 낼 수 있다 — 답안이 모두 이 기기에 있다. `gameRank()` 가
참가자별 평균 점수로 세우고, 이름을 누르면 그 사람의 잔별 상세로 들어간다.
정답(어느 잔이 어느 병인지)은 순위표에 한 번만 공개한다.

## 15. 제한시간 — 30초~10분

> ⚠ 이 앱에는 **타이머 기반 동작을 두지 않는 원칙**이 있다(동기화에서 지웠다).
> 이것은 예외이지만 성격이 다르다 — 사용자가 명시적으로 켠, 화면이 열려 있는 동안만
> 도는 **유한한** 카운트다운이고 만료되면 스스로 멈춘다.

30초 단위로 10분까지(`GAME_TIMES` 14단계). 뒤에 하는 사람이 더 오래 굴릴 수 있으면
순서가 유리해지므로 공정성 장치다. 만료되면 **입력한 것을 저장하고** 다음 차례로
넘긴다 — 시간이 끝났다고 입력을 버리면 억울하다. 남은 30초는 붉게 표시한다.

**누수가 유일한 위험이다.** 시계가 남아 돌면 다른 화면에서 갑자기 차례가 넘어간다.
그래서 `openSheet` 에서 **무조건** 끈다 — 이 앱의 모든 화면 이동이 그 함수를 지나므로
정리를 한 곳에 두면 빠뜨릴 경로가 없다. `closeSheet` 에서도 끈다.

실측: 제한시간 3초로 낮춰 실제로 만료시켰더니 자동으로 다음 차례(`{gi:0, pi:1}`)로
넘어가고 **직전에 고른 `바닐라` 가 답안에 보존**되었다. 화면을 옮기면 `GCLOCK` 이
`null` 이 되는 것도 확인했다.

## 가져오지 않은 것

- **네트워크 멀티 · TV 화면 · 참가자별 기기**(BlindSip) — 서버가 필요하다.
  **리더보드는 서버 없이 냈다**(§14) — 기기 하나를 돌려 쓰므로 답안이 모두 여기 있다.
  각자 자기 폰으로 동시에 입력하는 것만 서버의 몫으로 남았다.
- **배지 50종 · 연속 기록**([Drammer](https://play.google.com/store/apps/details?id=com.drammer.app) ·
  Untappd 계열) — 리텐션 장치다. **하루 한 잔 마시는 앱에 연속 기록을 붙이면 마시라고
  압박하는 앱이 된다.** 술 앱에서는 넣지 않는다.
- **Double Blind**(후보 목록 없이 전체 DB 에서 맞히기) — 구현은 되지만 229개에서 찍는
  것이 셀프 모드에서 즐겁지 않다. 필요해지면 난이도 레버로 넣는다.

## 지키는 것

- 서버·AI 없이 동작한다. 오프라인에서 된다.
- 게임 데이터는 컬렉션과 **분리**한다(`wc.game`). 통계·평점·잔량에 영향이 없다.
- 새 클래스 이름은 추가 전에 `grep`(실록 C-1).
- 가로 스크롤을 새로 만들지 않는다(v1.4.1 에서 PC 도달 불가 문제를 겪었다).
- 재지 않은 것을 됐다고 적지 않는다(V-6).
