# 개선 계획 — Gemini 로 초안 채우기

작성 2026-08-07. 요청: **새 위스키를 추가할 때 Gemini 무료 API 에 필드를 보내고, 받은 값으로
자동으로 채우기.**

결론부터 — **된다.** 다만 이 앱에서는 "부를 수 있는가"가 아니라 **"틀린 값을 어떻게 막는가"**
가 설계의 전부다. 아래는 그 순서로 정리했다.

---

## 0. 먼저 확인한 것 (측정)

**CORS 는 열려 있다.** `http://localhost:8765` 에서
`generativelanguage.googleapis.com/v1beta/models/…:generateContent` 로 POST 해
**응답 본문을 읽었다**(잘못된 키를 넣어 400 `API key not valid` 를 받았고, 그것이 읽혔다는
사실이 CORS 허용의 증거다). 즉 **서버도 프록시도 필요 없고 `fetch` 한 번이라 무의존
원칙이 깨지지 않는다.**

> 남은 확인: **배포 오리진**(`https://ysparkbc37d.github.io`)에서 한 번 더 재야 한다.
> Google 이 오리진을 가리지는 않는 것으로 보이지만, 재지 않은 것을 됐다고 적지 않는다(V-6).

**붙일 자리도 이미 있다.** 지금 초안 흐름은
`draftFrom()`(결정적) → `enrich()`(위키·커먼즈, 비동기) → `openEdit(null, draft)` 이고,
`draft.keys` 에 담긴 필드가 **`초안` 배지**(`.dpill`)를 받고 편집하면 배지가 사라지며
항목에 `draft:'1'` 이 남는다. **"이 값은 추측이다"를 표시하는 수단이 이미 완성되어 있다.**
AI 는 `enrich` 옆에 **네 번째 검색원**으로 들어간다.

```
① 내 컬렉션(이름 완전일치)  ② kb.js  ③ Wikipedia/Commons  ④ Gemini  ← 새로 추가
```

**순서가 곧 신뢰도**다. AI 는 맨 뒤이고 **앞 단계가 채운 값을 절대 덮지 않는다.**

---

## 1. 진짜 문제 — 환각

기술이 아니라 데이터 문제다. 이 앱의 초안 엔진이 결정적인 것은 의도된 선택이고,
핸드오프에도 **"도수 없는 병(약 38개) — 라벨 사진이 필요하다. 추측해 채우지 않는다"** 고
적혀 있다. LLM 은 도수·캐스크·연수를 자신 있게 지어낸다.

그리고 이 앱에는 **틀린 값이 조용히 번지는 경로**가 있다:

| 틀린 값 | 결과 |
| :--- | :--- |
| 도수 | 통계의 평균 도수, `기록 미완성` 필터 |
| 시장가 | **평가 손익**, **가성비순 정렬**, 위시 술장의 **예상 지출** |
| 구매가 | 총 구매액, 연도별 구매, 평균 구매가, 가성비순 |

**틀린 도수는 눈에 보이지만 틀린 시장가는 안 보인다.** 술장 통계가 전부 거기서 나온다.

### 필드 등급 (2026-08-07 확정)

사용자 결정: **도수·연수도 자동으로 채우고 `초안` 으로 표기해 사용자가 확인한다.**
(권고는 "제안만" 이었으나, 확인 절차가 이미 배지로 있으므로 채우는 쪽을 택했다.)

| 등급 | 필드 | 이유 |
| :--- | :--- | :--- |
| **채운다** | `name_en` `type` `distillery` `country` `region` `abv` `age` `cask` `bottler` `size_ml` `nose` `taste` `finish` `flavors` `tags` · **`market_low` `market_high` `rating_wb`** · **`notes`**(덧붙임) | 전부 `초안` 배지를 달고 들어간다. 사용자가 라벨을 보고 확인·수정한다 |
| **건드리지 않는다** | `purchase_price` `purchase_date` `purchase_place` `rating_my` `status` `remaining_pct` `opened_at` `storage` | **내 기록**이다 — 얼마에 어디서 샀고 지금 얼마 남았는지는 AI 가 알 수 없다 |

**2차 갱신 (2026-08-07, 사용자 요청)** — 처음에는 시장가와 평점을 전부 뺐지만
`market_low`·`market_high`·`rating_wb` 를 **넣기로 했다.** 위험은 그대로다(아래) —
`초안` 배지와 사용자 확인을 전제로 받아들인 결정이다.
방어는 범위 검사로 한다: 원화 5천원~1억을 벗어난 값, 100점 척도를 벗어난 평점은 버린다
(모델이 파운드 값이나 10점 척도로 답하는 경우가 실제로 있다).
**그럴듯한 범위의 틀린 값은 걸러낼 수 없다** — 그건 사용자 확인에 맡긴다.

`storage` 는 AI 가 채우지 않지만 **새 병의 기본값을 `집`** 으로 둔다(사용자 요청).
`draftFrom()` 과 `openEdit(null)` 에만 넣는다 — `normalize()` 에 넣으면 기존 142병의
빈 보관 위치까지 소급해서 바뀐다.

### 초안 값도 다시 채운다 (2026-08-07)

처음에는 **빈 칸만** 채웠다. 그런데 내장 DB 에 없는 병은 초안 엔진이 근사 일치로
**다른 병의 값을 넣어 두고**, 그 칸이 "비어 있지 않다"는 이유로 건너뛰어졌다 —
틀린 값이 그대로 남는다(사용자가 실제로 겪었다).

`초안` 배지는 **아직 확인되지 않았다**는 뜻이므로 다시 채워도 된다.
대상 = **빈 칸 ∪ 아직 `dk` 에 있는 칸**. 사용자가 직접 고친 칸은 배지가 떨어져
`dk` 에서 빠지므로 자동으로 보호된다 — 판단 기준을 새로 만들지 않았다.

그래서 되돌리기도 "비우기"에서 **"덮기 전 값으로 복원"** 으로 바뀌었다(`aiPrev`).
초안 엔진 값으로 돌아갔으면 배지도 `초안 · AI` → `초안` 으로 되돌린다.

### 메모는 덮지 않고 덧붙인다 (2026-08-07)

`notes` 를 넣었지만 **덮으면 안 된다** — 초안 엔진이 여기에
`⚠ 캐스크 스트렝스 — 도수는 배치/병별로 다릅니다` 같은 **경고**를 넣기 때문에,
덮으면 사용자가 알아야 할 주의사항이 조용히 사라진다.
`setVal` 이 `notes` 만 특별히 다뤄 아래에 한 줄 덧붙이고, 같은 문장이 이미 있으면
넣지 않는다(두 번 눌러도 중복되지 않는다). 되돌리기는 덧붙인 부분만 떼어 낸다.

### 들어올 때 먼저 묻는다 (2026-08-07, 사용자 요청)

버튼이 있다는 것만으로는 부족했다 — 초안 값은 확인해야 하는 값인데 화면에 들어오면
그냥 저장하게 된다. 그래서 **새 병·초안 병의 편집 화면에 들어오면 폼 맨 위에서 묻는다**:
`확인이 필요한 칸이 n개 있습니다. AI로 다시 채워 볼까요? [지금 채우기] [나중에]`

**시트 위에 모달을 띄우지 않는다.** 이 앱의 `confirm()` 은 되돌릴 수 없는 작업 전용이고,
이미 열린 시트 위의 모달은 본문 잠금·자판 처리와 엉킨다(실록 C-9 · C-10).
`.airow.ask` 로 그 줄을 강조하는 것으로 대신한다 — 놓칠 수 없는 자리이고 답하지 않아도
막지 않는다. **이미 확인을 끝낸 병(`draft !== '1'`)에서는 묻지 않는다** — 매번 뜨면 잔소리다.
키가 없으면 묻는 대신 설정으로 안내한다.

`name_ko` 는 사용자가 입력한 값이므로 고치지 않는다. `name_en` 은 비어 있을 때만 채운다.

**시장가를 뺀 이유를 다시 적어 둔다** — 평가 손익·가성비순 정렬·위시 술장 예상 지출이
전부 시장가에서 나온다. 도수가 틀리면 사용자가 라벨을 보고 알아채지만, 시장가가 틀리면
아무 화면에도 이상이 나타나지 않는다. `기록 미완성` 필터가 시장가 없는 병을 이미 모아
주므로, 비워 두는 것이 오답보다 낫다.

`name_ko`/`name_en` 은 사용자가 입력한 것이므로 AI 가 고치지 않는다 —
단 **표기 교정 제안**(`글렌알라키` → `GlenAllachie`)은 `name_en` 이 비어 있을 때만 채운다.

### 값의 출처를 남긴다

지금 `초안` 배지는 출처를 구분하지 않는다. AI 값은 **어디서 왔는지 보여야** 사용자가
판단할 수 있다. `draft.src` 를 두어 배지 문구를 나눈다: `초안` / `초안 · AI`.
`draft:'1'` 은 그대로 두고(기존 필터·통계가 쓴다), 출처는 표시 전용으로만 쓴다.

---

## 2. API 키 — 앱에 넣을 수 없다

저장소가 공개이고 앱이 클라이언트 전용이라, 키를 심으면 그대로 노출된다. 남이 쓰거나
Google 이 막는다. **사용자 본인 키만 가능하다.**

다행히 이 앱에는 **이미 같은 패턴이 있다** — GitHub PAT(`wc.token`)를 설정에서 받아
`localStorage` 에 두고, `sync.html` 로 발급 방법을 안내한다. 그대로 따른다:

| | 동기화(기존) | AI 초안(신규) |
| :--- | :--- | :--- |
| 키 저장 | `wc.token` | `wc.aikey` |
| 헬퍼 | `ghToken()` | `aiKey()` |
| 켜짐 판정 | `syncOn()` | `aiOn()` |
| 설정 위치 | `openData()` 의 `기기 간 동기화` | 같은 시트에 `AI 초안` 섹션 |
| 안내 문서 | `sync.html` | `ai.html` (새로 만든다, SHELL 에 추가) |

- 키는 `password` 타입 입력으로 받고, 저장 후에는 앞 11자·뒤 4자만 보여 준다
  (동기화 토큰과 같은 방식).
- **키가 없으면 기능 자체를 숨기지 않고** 설정에 안내를 둔다 — 동기화와 같다.
  "선택 기능이며 없어도 모든 기능을 쓸 수 있다"가 이 앱의 태도다.
- 모델 이름도 입력값으로 둔다(`wc.aimodel`, 기본 `gemini-2.0-flash`). 무료 등급의
  모델 구성은 바뀌므로 코드에 박으면 어느 날 조용히 실패한다.

---

## 3. 사용자가 쓰던 프롬프트에서 가져올 것

사용자가 이미 챗 UI 에서 쓰던 프롬프트가 있다. **출력 템플릿이 이 앱의 `FIELDS` 와
거의 그대로 일치하고**, 맛 프로필 목록이 앱의 `FLAVORS`
(`피트·셰리·과일·버번·와인·라이`)와 **정확히 같다.** 그대로 살릴 것과 버릴 것을 가른다.

### 가져온다

| 항목 | 왜 |
| :--- | :--- |
| **출처를 `메모` 에 남긴다** | 이 프롬프트에서 가장 좋은 아이디어다. AI 의 주장을 **검증 가능하게** 만든다. 앱은 이미 사진 출처를 `image_credit` 에 남기는 습관이 있으니 결이 같다. 단 §3-1 의 함정을 먼저 읽어라 |
| **표기 규격을 프롬프트에 못박는다** | 도수 `53.9`, 숙성 `14` 또는 `NAS`, 용량 `700`, 가격은 쉼표 없이, 날짜 `YYYY-MM-DD`. 앱의 필드가 기대하는 형태와 같다. 뒤에서 고치는 것보다 **애초에 그 형태로 받는 것**이 실패가 적다 |
| **배치·빈티지 정보가 없으면 표준 OB 기준** | 앱의 `bottler` 기본값이 `Official (OB)` 이고, 캐스크 스트렝스·싱글 배럴에는 이미 "병마다 다르다" 경고를 붙인다. 규칙이 일치한다 |
| **맛 프로필은 목록 안에서만** | 앱의 `FLAVORS` 6개를 프롬프트에 넣는다. 이미 계획에 있던 것이고, 사용자 프롬프트가 같은 결론에 도달해 있었다 |
| **정보 확인 불가 필드는 빈값** | 계획의 "모르면 빈 문자열" 과 같다 |

### 가져오지 않는다

| 항목 | 왜 |
| :--- | :--- |
| 기본값 `보관 위치="Home"`, `상태="미개봉"`, `잔량=100%` | **앱이 이미 한다** — `draftFrom()` 이 `status='미개봉'`, `remaining_pct='100'`, `size_ml='700'`, `purchase_date=today()` 를 넣는다. 그리고 **보관 위치는 자유 입력이 아니라 집·바·기타 칩으로 바뀌었다**(v1.3.0) — `"Home"` 은 이제 맞지 않는 값이다. 이 세 필드는 §1 의 "건드리지 않는다" 에 있다 |
| 마크다운 코드 블록 1개로 출력 | 챗에서 읽을 때는 맞지만 앱에서는 파싱이 하나 더 생긴다. §4 의 JSON 스키마가 그 자리를 대신한다 — **단 §3-1 의 결과에 따라 이 판단이 뒤집힐 수 있다** |
| 입력 누락 시 안내 문장만 출력 | 앱은 이름 없이 이 단계에 오지 않는다. 위저드가 이미 막는다 |

### 3-1. ⚠ 이 프롬프트가 가정한 것 중 API 에서 성립하지 않는 것

프롬프트는 **"실시간 검색을 수행하고"**, **"Whiskybase 평점과 정보 출처(Source URL)를
반드시 확보"**, **"사실 검증이 완료된 데이터만"** 을 요구한다.
챗 UI(Gemini 앱·AI Studio)에서는 검색이 붙어 있어 성립한다.
**그런데 `generateContent` 를 그냥 부르면 검색이 없다.** 모델은 기억으로 답한다.

그래서 프롬프트를 그대로 옮기면 **`메모` 의 Whiskybase URL 이 지어내진다.**
출처가 없는 것보다 나쁘다 — **가짜 출처는 검증된 것처럼 보인다.**

> **확정 (2026-08-07): 검색 도구를 붙이지 않는다.** 따라서
> **출처 URL 을 AI 에게 요구하지 않는다** — 지어낸 URL 이 검증된 것처럼 보이는 쪽이
> 출처가 없는 쪽보다 나쁘다. `메모` 에는 `AI 초안 — 라벨과 Whiskybase 확인 필요` 만
> 적고, 대조는 편집 폼에 **이미 있는** `Whiskybase`·`구글`·`네이버` 버튼
> (`findW`/`findG`/`findN`)으로 사용자가 한다.
> JSON 스키마(§4)는 그대로 쓴다 — 도구를 붙이지 않으므로 충돌이 없다.

아래 표는 그 판단의 근거로 남긴다:

| | 검색 그라운딩 켬 | 끔 (기본) |
| :--- | :--- | :--- |
| 방법 | 요청에 Google 검색 도구를 붙인다 | 그냥 호출 |
| 사실성 | 높다. URL 이 실재한다 | 기억에 의존. **URL 을 요구하면 안 된다** |
| 구조화 출력 | **`responseSchema` 와 함께 쓸 수 있는지 불확실하다.** 도구를 붙이면 스키마가 거부될 수 있다 → 그때는 **사용자의 마크다운 방식이 오히려 맞다**(텍스트로 받아 앱이 파싱) |
| 무료 한도 | 검색 호출이 별도로 계산될 수 있다 | 단순 |

**끔으로 갈 경우의 출처 처리** — AI 에게 URL 을 받지 않는다. 대신 `메모` 에
`AI 초안 — 라벨과 Whiskybase 확인 필요` 만 적고, **검증은 이미 있는 버튼으로 넘긴다.**
편집 폼에는 `Whiskybase` · `구글` · `네이버` 검색 버튼(`findW`/`findG`/`findN`)이 이미 있다.
사용자가 한 번 눌러 대조하는 것이, 모델이 만든 URL 을 믿는 것보다 낫다.

## 4. 마크다운 왕복이 아니라 JSON 스키마

원래 아이디어는 "파라미터 마크다운을 보내고 피드백을 파싱"이었다. 응답 형식이 흔들리면
깨지므로, Gemini 의 **구조화 출력**을 쓴다 — `responseMimeType: "application/json"` 과
`responseSchema` 를 주면 파싱이 사라진다.

```
POST /v1beta/models/<model>:generateContent?key=<사용자 키>
{
  contents: [{ parts: [{ text: <프롬프트> }] }],
  generationConfig: {
    responseMimeType: "application/json",
    responseSchema: { type:"OBJECT", properties: { distillery:{type:"STRING"}, … } },
    temperature: 0            // 같은 이름에 같은 답이 나와야 한다
  }
}
```

프롬프트에 박아야 하는 것:

- **모르면 빈 문자열.** "추측하지 말라"가 아니라 **"모르는 항목은 `""` 로 두라"** 고
  형식으로 강제한다. 지어낼 자리를 없애는 쪽이 말로 부탁하는 것보다 낫다.
- 한국어 표기 규칙(증류소는 통용 표기), 서술 필드는 **쉼표로 구분된 짧은 항목**
  (`flavors`·`tags` 는 앱의 기존 값 목록 안에서만 고르게 한다 — `FLAVORS` 를 프롬프트에 넣는다).
- `temperature: 0`. 같은 병을 두 번 넣었을 때 다른 답이 나오면 사용자가 신뢰하지 않는다.

응답을 받은 뒤 **앱이 다시 검사한다** — 모델을 믿지 않는다:

1. 스키마에 없는 키는 버린다.
2. `type` 은 `TYPES` 안의 값만, `flavors` 는 `FLAVORS` 안의 값만 통과시킨다.
3. **이미 값이 있는 필드는 건너뛴다.** 앞 단계(내 기록·kb.js·위키)가 이긴다.
4. 길이 제한(서술 필드 300자 등). 긴 문장은 잘라 넣지 않고 **버린다** — 잘린 문장은
   사용자가 고치기 더 번거롭다.

---

## 5. 언제 부르는가

**사용자가 누를 때만** 부른다(권고). 위저드에서 이름을 넣으면 지금처럼 ①②③ 이 돌고,
결과 로그 아래에 **`AI로 더 채우기`** 버튼이 붙는다. 이유:

- 무료 등급에는 한도가 있다. 병을 넣을 때마다 자동으로 부르면 한도를 언제 쓰는지
  사용자가 통제할 수 없다.
- ①②③ 으로 충분히 채워지는 병(내장 DB 295곳)에서는 부를 이유가 없다.
- **네트워크 실패·한도 초과가 등록 자체를 막으면 안 된다.** 버튼이면 실패해도
  등록 흐름이 살아 있다.

`enrich()` 의 `if (!navigator.onLine) return out;` 패턴을 그대로 따른다 —
오프라인이면 버튼을 비활성으로 두고 이유를 적는다.

---

## 6. 실패를 어떻게 다루는가

| 상황 | 동작 |
| :--- | :--- |
| 키 없음 | 버튼에 `설정에서 API 키를 넣어 주세요` + 설정으로 가는 링크 |
| 오프라인 | 버튼 비활성, `오프라인입니다` |
| 400 (키 오류) | 응답의 `error.message` 를 그대로 보여 준다 — 삼켜서 "실패"로만 적으면 원인을 알 수 없다 |
| 429 (한도) | `무료 한도를 넘었습니다. 잠시 뒤 다시 시도하세요` |
| JSON 파싱 실패 | 조용히 아무것도 채우지 않는다. 부분 적용이 더 나쁘다 |
| 값이 틀렸다 | **`AI 값 되돌리기`** — 이번 호출로 채운 키만 원래대로 비운다. 그래서 채운 키 목록을 들고 있어야 한다 |

---

## 7. 개인정보처리방침

지금 `privacy.html` 은 **"제작자에게 전송되지 않습니다"** 라고 적고 있고, 그 문서 §3 에
**외부로 나가는 통신 표**가 이미 있다(GitHub Pages·Wikipedia·Wikimedia·사용자 저장소).
**행 하나를 추가하는 것으로 끝난다** — 문서를 다시 쓸 필요는 없다.

적어야 하는 것:

- 무엇이 나가는가: **사용자가 입력한 위스키 이름과 필드 이름뿐.** 컬렉션·구매 기록·사진은
  보내지 않는다.
- 누구에게: Google (Generative Language API). **사용자 본인의 API 키로** 직접 호출하며
  제작자의 서버를 거치지 않는다.
- **무료 등급은 입력이 모델 개선에 쓰일 수 있다**(Google 정책). 이 문장을 빼면 안 된다.
- 끄는 방법: 설정에서 키를 지우면 통신이 사라진다. **기본은 꺼짐**이다.

`ai.html` 에도 같은 내용을 요약해 둔다.

---

## 8. 얼마나 맞히는지 어떻게 아는가 (이 계획의 핵심 검증)

"쓸 만하다"는 느낌으로 판단하면 안 된다. **이미 정답을 아는 데이터가 142병 있다.**

1. 사용자 컬렉션에서 `abv`·`type`·`distillery`·`region`·`cask` 가 **채워져 있는** 병을
   20~30개 고른다(사용자가 라벨을 보고 넣은 값이므로 정답으로 쓴다).
2. 각 병의 **이름만** AI 에 넣어 응답을 받는다.
3. 필드별로 **일치·불일치·빈값**을 센다. 결과를 표로 남긴다.
4. **출처 URL 을 요구해 그것이 실제로 열리는지 확인한다**(§3-1). 안 열리는 URL 이 나오면
   그라운딩 없이는 출처를 받지 않기로 확정한다. 이것이 환각을 재는 가장 직접적인 시험이다.
5. 그 표를 보고 필드 등급을 **조정한다.** `abv` 가 자주 틀리면 채우기에서 내리고,
   `cask` 가 절반만 맞으면 문구를 달아 둔다.

이 측정은 **1단계 이전에** 할 수 있다 — 스크래치패드에서 호출 한 번씩 돌려 보면 된다.
키가 있어야 하므로 사용자에게 받아야 하고, **키는 저장소·문서·커밋·로그에 절대 넣지
않는다.** 측정에 쓰는 값은 실제 컬렉션의 이름뿐이고, 구매 기록은 보내지 않는다.

---

## 9. 단계

**순서를 바꿨다 (2026-08-07 결정).** 0단계 측정은 API 키가 있어야 하고, 키는 사용자
것이다. 그래서 **1~3단계를 먼저 만들어 설정에 키 칸을 두고, 사용자가 직접 몇 병 돌려 본
결과로 §1 의 필드 등급을 조정한다.** 측정을 건너뛰는 것이 아니라 **측정하는 사람을
바꾸는** 것이다.

| 단계 | 내용 | 검증 |
| :--- | :--- | :--- |
| **1** | 설정에 `AI 초안` 섹션 — 키·모델 저장, `aiKey()`/`aiModel()`/`aiOn()` | 키 없음/있음/잘못된 키 세 경우 |
| **2** | `aiFill(name, 빈필드)` — 호출·스키마·재검사(§4). 화면 없이 함수만 | 잘못된 키로 오류 경로, 응답 검사가 규격 밖 값을 버리는지 |
| **3** | 편집 폼에 `AI로 빈 칸 채우기` + `초안 · AI` 배지 + `되돌리기` | 실패 6경우(§6) 각각 재현 |
| **4** | `privacy.html` 행 추가 + `ai.html` + SHELL·CHANGELOG·버전 | 배포 오리진에서 CORS 재확인 |
| **5** | 사용자 측정 결과로 필드 등급 조정(§8) | 필드별 일치율 |

**버튼은 위저드가 아니라 편집 폼에 둔다.** 위저드는 이름을 넣으면 곧 편집 폼으로
넘어가므로 위저드에 두면 사라진다. 편집 폼에 두면 **이미 등록한 병에도 쓸 수 있다** —
도수가 비어 있는 38병이 바로 그 대상이다. `초안` 배지 구조(`dk`)도 편집 폼에 있다.

---

## 지키는 것

- **무의존.** SDK 를 넣지 않는다. `fetch` 하나로 끝낸다(CDN 은 오프라인을 깬다).
- **AI 는 맨 뒤 검색원이고, 앞 단계 값을 덮지 않는다.**
- **돈·평점·상태 필드는 건드리지 않는다.** 술장 통계가 여기서 나온다.
- 키는 사용자 것이고 `localStorage` 에만 둔다. **저장소·문서·커밋·로그에 남기지 않는다.**
- 기본은 **꺼짐**. 키가 없어도 앱의 모든 기능이 지금처럼 동작한다.
- 새 클래스 이름은 추가 전에 `grep`(실록 C-1). 셸 파일을 고쳤으면 `sw.js` 의 `V` 를 올린다.
- **재지 않은 것을 됐다고 적지 않는다**(V-6). 배포 오리진 CORS, 정확도, 무료 한도는
  전부 실측 항목이다.
